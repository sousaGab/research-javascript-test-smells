file,type,line,method,source
/app/helpers/__tests__/form.spec.js,IdenticalTestDescription,"{'startLine':642,'endLine':646}","it('should pass when not required', () => {
    const currency = {};
    const validation = validateCurrency(false, currency);
    expect(validation).toEqual(true);
  })",snuts
/app/helpers/__tests__/form.spec.js,IdenticalTestDescription,"{'startLine':675,'endLine':679}","it('should pass when not required', () => {
    const discount = {};
    const validation = validateDiscount(false, discount);
    expect(validation).toEqual(true);
  })",snuts
/app/helpers/__tests__/form.spec.js,IdenticalTestDescription,"{'startLine':721,'endLine':725}","it('should pass when not required', () => {
    const tax = {};
    const validation = validateTax(false, tax);
    expect(validation).toEqual(true);
  })",snuts
/app/helpers/__tests__/form.spec.js,IdenticalTestDescription,"{'startLine':750,'endLine':754}","it('should pass when not required', () => {
    const note = {};
    const validation = validateNote(false, note);
    expect(validation).toEqual(true);
  })",snuts
/app/helpers/__tests__/form.spec.js,VerboseStatement,"{'startLine':128,'endLine':149}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",snuts
/app/reducers/__tests__/SettingsReducer.spec.js,AnonymousTest,"{'startLine':89,'endLine':105}","it('general settings', () => {
    const data = {
      muted: false,
      sound: 'modern',
      lang: 'vi',
    };
    const newState = SettingsReducer(currentState, {
      type: ACTION_TYPES.SETTINGS_UPDATE,
      payload: {
        setting: 'general',
        data,
      },
    });
    expect(newState.current.general.muted).toBeFalsy;
    expect(newState.current.general.sound).toEqual('modern');
    expect(newState.current.general.lang).toEqual('vi');
  })",snuts
/app/reducers/__tests__/SettingsReducer.spec.js,AnonymousTest,"{'startLine':107,'endLine':129}","it('profile settings', () => {
    const data = {
      fullname: 'Jon Snow',
      company: 'HBO',
      address: 'Winterfell',
      email: 'jon@snow.com',
      website: 'https://iknownothing.com',
    };
    const newState = SettingsReducer(currentState, {
      type: ACTION_TYPES.SETTINGS_UPDATE,
      payload: {
        setting: 'profile',
        data,
      },
    });
    expect(newState.current.profile.fullname).toEqual('Jon Snow');
    expect(newState.current.profile.company).toEqual('HBO');
    expect(newState.current.profile.address).toEqual('Winterfell');
    expect(newState.current.profile.email).toEqual('jon@snow.com');
    expect(newState.current.profile.website).toEqual(
      'https://iknownothing.com'
    );
  })",snuts
/app/reducers/__tests__/SettingsReducer.spec.js,AnonymousTest,"{'startLine':131,'endLine':147}","it('invoice settings', () => {
    const data = {
      template: 'business',
      currency: 'THB',
      dateFormat: 'MMMM/DDDD/YYYY',
    };
    const newState = SettingsReducer(currentState, {
      type: ACTION_TYPES.SETTINGS_UPDATE,
      payload: {
        setting: 'invoice',
        data,
      },
    });
    expect(newState.current.invoice.template).toEqual('business');
    expect(newState.current.invoice.currency).toEqual('THB');
    expect(newState.current.invoice.dateFormat).toEqual(data.dateFormat);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':172,'endLine':178}","it('currency field', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_TOGGLE,
      payload: 'currency',
    });
    expect(newState.settings.required_fields.currency).toBe(true);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':180,'endLine':186}","it('dueDate field', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_TOGGLE,
      payload: 'dueDate',
    });
    expect(newState.settings.required_fields.dueDate).toBe(true);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':188,'endLine':194}","it('discount field', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_TOGGLE,
      payload: 'discount',
    });
    expect(newState.settings.required_fields.discount).toBe(true);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':196,'endLine':202}","it('note field', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_TOGGLE,
      payload: 'note',
    });
    expect(newState.settings.required_fields.note).toBe(true);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':204,'endLine':210}","it('tax field', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_TOGGLE,
      payload: 'tax',
    });
    expect(newState.settings.required_fields.tax).toBe(true);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':225,'endLine':252}","it('recipient data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_RECIPIENT_UPDATE,
      payload: {
        newRecipient: false,
        select: {
          fullname: 'Jon Snow',
          company: 'HBO',
          email: 'jon@snow.got',
          phone: '000000000',
        },
        new: {},
      },
    });
    expect(newState.recipient.newRecipient).toBe(false);
    expect(newState.recipient.select).toEqual({
      fullname: 'Jon Snow',
      company: 'HBO',
      email: 'jon@snow.got',
      phone: '000000000',
    });
    expect(newState.recipient.select).not.toEqual({
      fullname: 'Dany',
      company: 'HBO',
      email: 'mother@dragons.got',
      phone: '111111111',
    });
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':254,'endLine':274}","it('currency data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'currency',
        data: {
          selectedCurrency: {
            symbol: '$',
            name: 'US Dollar',
            symbol_native: '$',
            decimal_digits: 2,
            rounding: 0,
            code: 'USD',
            name_plural: 'US dollars',
          },
        },
      },
    });
    expect(newState.currency.selectedCurrency.code).not.toEqual('VND');
    expect(newState.currency.selectedCurrency.code).toEqual('USD');
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':276,'endLine':287}","it('dueDate data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'dueDate',
        data: {
          selectedDate: '28/07/1988',
        },
      },
    });
    expect(newState.dueDate.selectedDate).toEqual('28/07/1988');
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':289,'endLine':304}","it('discount data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'discount',
        data: {
          type: 'percentage',
          amount: 10,
        },
      },
    });
    expect(newState.discount.type).toEqual('percentage');
    expect(newState.discount.type).not.toEqual('flat');
    expect(newState.discount.amount).toEqual(10);
    expect(newState.discount.amount).not.toEqual(5);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':306,'endLine':318}","it('tax data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'tax',
        data: {
          amount: 5,
        },
      },
    });
    expect(newState.tax.amount).toEqual(5);
    expect(newState.tax.amount).not.toEqual(10);
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,AnonymousTest,"{'startLine':320,'endLine':334}","it('note data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'note',
        data: {
          content: 'You know nothing, Jon Snow',
        },
      },
    });
    expect(newState.note.content).toEqual('You know nothing, Jon Snow');
    expect(newState.note.content).not.toEqual(
      'The Lannisters always pay their debts'
    );
  })",snuts
/app/reducers/__tests__/FormReducer.spec.js,OvercommentedTest,"{'startLine':482,'endLine':496}","it('should populate other fields data correctly', () => {
    // Rows
    expect(newState.rows.length).toEqual(1);
    expect(newState.rows).toEqual(invoiceData.rows);
    // Tax
    expect(newState.tax).toEqual(invoiceData.tax);
    // Currency
    expect(newState.currency).toEqual(invoiceData.currency);
    // Note
    expect(newState.note.content).toEqual(invoiceData.note);
    // Discount
    expect(newState.discount).toEqual(invoiceData.discount);
    // DueDate
    expect(newState.dueDate).toEqual(invoiceData.dueDate);
  })",snuts
/app/containers/__tests__/Invoices.spec.js,AnonymousTest,"{'startLine':85,'endLine':90}","it('matches snapshot', () => {
    const tree = renderer
      .create(<Invoices t={t} dispatch={dispatch} invoices={invoices} />)
      .toJSON();
    expect(tree).toMatchSnapshot();
  })",snuts
/app/containers/__tests__/Invoices.spec.js,GeneralFixture,"{'startLine':100,'endLine':100}","beforeEach(() => {
    spySetFilter = jest.spyOn(Invoices.prototype, 'setFilter');
    wrapper = mount(
      <Invoices t={t} invoices={invoices} dispatch={dispatch} />
    );
    actions = wrapper.find(PageHeaderActions).first();
    filterBtn = actions.find(Button).last();
    filterBtn.simulate('click', {
      target: {
        dataset: {
          filter: 'paid',
        },
      },
    });
  })",snuts
/app/containers/__tests__/Invoices.spec.js,GeneralFixture,"{'startLine':101,'endLine':101}","beforeEach(() => {
    spySetFilter = jest.spyOn(Invoices.prototype, 'setFilter');
    wrapper = mount(
      <Invoices t={t} invoices={invoices} dispatch={dispatch} />
    );
    actions = wrapper.find(PageHeaderActions).first();
    filterBtn = actions.find(Button).last();
    filterBtn.simulate('click', {
      target: {
        dataset: {
          filter: 'paid',
        },
      },
    });
  })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,IdenticalTestDescription,"{'startLine':61,'endLine':76}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti('warning', 'Missing Doc');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('WARNING');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':25,'endLine':25}","it('should NOT update active tab', () => {
      middleware(UIActions.changeActiveTab('form'));
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':26,'endLine':26}","it('should NOT update active tab', () => {
      middleware(UIActions.changeActiveTab('form'));
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':32,'endLine':32}","it('should update active tab', () => {
      const action = UIActions.changeActiveTab('settings');
      middleware(action);
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Test Sound
      expect(sounds.play).toBeCalledWith('TAP');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':33,'endLine':33}","it('should update active tab', () => {
      const action = UIActions.changeActiveTab('settings');
      middleware(action);
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Test Sound
      expect(sounds.play).toBeCalledWith('TAP');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':47,'endLine':47}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti(
        'success',
        'Invoice has been created successfully!'
      );
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('SUCCESS');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':48,'endLine':48}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti(
        'success',
        'Invoice has been created successfully!'
      );
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('SUCCESS');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':64,'endLine':64}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti('warning', 'Missing Doc');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('WARNING');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':65,'endLine':65}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti('warning', 'Missing Doc');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('WARNING');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':83,'endLine':83}","it('should handle FORM_ITEM_ADD action', () => {
      const action = FormActions.addItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('ADD');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':84,'endLine':84}","it('should handle FORM_ITEM_ADD action', () => {
      const action = FormActions.addItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('ADD');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':92,'endLine':92}","it('should handle FORM_ITEM_REMOVE action', () => {
      const action = FormActions.removeItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('REMOVE');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':93,'endLine':93}","it('should handle FORM_ITEM_REMOVE action', () => {
      const action = FormActions.removeItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('REMOVE');
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':101,'endLine':101}","it('should handle FORM_CLEAR action WIHOUT sound', () => {
      const action = FormActions.clearForm('muted');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
    })",snuts
/app/middlewares/__tests__/UIMiddleware.spec.js,SubOptimalAssert,"{'startLine':102,'endLine':102}","it('should handle FORM_CLEAR action WIHOUT sound', () => {
      const action = FormActions.clearForm('muted');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
    })",snuts
/app/middlewares/__tests__/SettingsMW.spec.js,CommentsOnlyTest,"{'startLine':76,'endLine':76}","it('should save data', () => {})",snuts
/app/middlewares/__tests__/SettingsMW.spec.js,NonFunctionalStatement,"{'startLine':76,'endLine':76}","it('should save data', () => {})",snuts
/app/middlewares/__tests__/SettingsMW.spec.js,SubOptimalAssert,"{'startLine':31,'endLine':31}","it('should handle SETTINGS_GET_INITIAL action', () => {
    const savedSettings = {
      profile: 'someSettings',
      invoice: 'someSettings',
      general: 'someSettings',
    };
    const action = Actions.getInitalSettings();
    middleware(action);
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: {
          current: savedSettings,
          saved: savedSettings,
        },
      })
    );
  })",snuts
/app/middlewares/__tests__/SettingsMW.spec.js,SubOptimalAssert,"{'startLine':32,'endLine':32}","it('should handle SETTINGS_GET_INITIAL action', () => {
    const savedSettings = {
      profile: 'someSettings',
      invoice: 'someSettings',
      general: 'someSettings',
    };
    const action = Actions.getInitalSettings();
    middleware(action);
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: {
          current: savedSettings,
          saved: savedSettings,
        },
      })
    );
  })",snuts
/app/middlewares/__tests__/SettingsMW.spec.js,SubOptimalAssert,"{'startLine':100,'endLine':100}","it('should call next and dispatch notification', () => {
      middleware(action);
      // Call Next
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Send Notification
      expect(dispatch.mock.calls.length).toBe(1);
      expect(dispatch).toHaveBeenCalledWith({
        type: ACTION_TYPES.UI_NOTIFICATION_NEW,
        payload: {
          type: 'success',
          message: i18n.t('messages:settings:saved'),
        },
      });
    })",snuts
/app/middlewares/__tests__/SettingsMW.spec.js,SubOptimalAssert,"{'startLine':103,'endLine':103}","it('should call next and dispatch notification', () => {
      middleware(action);
      // Call Next
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Send Notification
      expect(dispatch.mock.calls.length).toBe(1);
      expect(dispatch).toHaveBeenCalledWith({
        type: ACTION_TYPES.UI_NOTIFICATION_NEW,
        payload: {
          type: 'success',
          message: i18n.t('messages:settings:saved'),
        },
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,IdenticalTestDescription,"{'startLine':224,'endLine':247}","it('handle unknown error correctly', () => {
      // Setup
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
      };
      const expectedError = new Error('Something Broken!');
      saveDoc.mockImplementationOnce(() => Promise.reject(expectedError));

      // Execute
      middleware(Actions.saveInvoice(newInvoice)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,IdenticalTestDescription,"{'startLine':382,'endLine':407}","it('should call next and dispatch notification ', () => {
      // Setup
      const invoiceID = 'jon-invoice';
      const action = Actions.deleteInvoice(invoiceID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,IdenticalTestDescription,"{'startLine':442,'endLine':458}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'ned-stark';
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.deleteInvoice(invoiceID)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,IdenticalTestDescription,"{'startLine':498,'endLine':517}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const configs = { color: 'red' };
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.saveInvoiceConfigs(invoiceID, configs)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,IdenticalTestDescription,"{'startLine':535,'endLine':554}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const status = 'paid';
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.setInvoiceStatus(invoiceID, status)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':52,'endLine':52}","it('calls next after the promised is returned', () => {
      const action = Actions.getInvoices();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Call next with resolved data
        return getAllDocs('invoices').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':55,'endLine':55}","it('calls next after the promised is returned', () => {
      const action = Actions.getInvoices();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Call next with resolved data
        return getAllDocs('invoices').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':154,'endLine':154}","it('should call next and dispatch notification ', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };

      // Execute
      const action = Actions.saveInvoice(newInvoice);
      middleware(action).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.invoicesRecords,
                newInvoice
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:saved'),
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':164,'endLine':164}","it('should call next and dispatch notification ', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };

      // Execute
      const action = Actions.saveInvoice(newInvoice);
      middleware(action).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.invoicesRecords,
                newInvoice
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:saved'),
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':275,'endLine':275}","it('should call next and dispatch change Tab action', () => {
      // Setup
      const currentInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          symbol: '$',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      // Execute
      const action = Actions.editInvoice(currentInvoice);
      middleware(action).then(() => {
        // Call next
        expect(next.mock.calls.length).toBe(1);
        expect(next).toHaveBeenCalledWith(
          Object.assign({}, action, {
            payload: Object.assign({}, action.payload, {
              contacts: mockData.contactsRecords
            })
          })
        );
        // Dispatch change Tab action
        expect(dispatch.mock.calls.length).toBe(1);
        expect(dispatch).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_TAB_CHANGE,
          payload: 'form'
        });
      })
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':284,'endLine':284}","it('should call next and dispatch change Tab action', () => {
      // Setup
      const currentInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          symbol: '$',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      // Execute
      const action = Actions.editInvoice(currentInvoice);
      middleware(action).then(() => {
        // Call next
        expect(next.mock.calls.length).toBe(1);
        expect(next).toHaveBeenCalledWith(
          Object.assign({}, action, {
            payload: Object.assign({}, action.payload, {
              contacts: mockData.contactsRecords
            })
          })
        );
        // Dispatch change Tab action
        expect(dispatch.mock.calls.length).toBe(1);
        expect(dispatch).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_TAB_CHANGE,
          payload: 'form'
        });
      })
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':328,'endLine':328}","it('should call next and dispatch notification', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() =>
        updateDoc('invoices', currentInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: mockData.invoicesRecords,
          });
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:updated'),
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':334,'endLine':334}","it('should call next and dispatch notification', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() =>
        updateDoc('invoices', currentInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: mockData.invoicesRecords,
          });
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:updated'),
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':390,'endLine':390}","it('should call next and dispatch notification ', () => {
      // Setup
      const invoiceID = 'jon-invoice';
      const action = Actions.deleteInvoice(invoiceID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':397,'endLine':397}","it('should call next and dispatch notification ', () => {
      // Setup
      const invoiceID = 'jon-invoice';
      const action = Actions.deleteInvoice(invoiceID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':425,'endLine':425}","it('should clear the form if this invoice is being editted', () => {
      const getState = jest.fn(() => ({
        form: {
          settings: {
            editMode: {
              active: true,
              data: { _id: 'jon-invoice' }
            }
          }
        }
      }));
      const middleware = InvoicesMW({ dispatch, getState })(next);
      const invoiceID = 'jon-invoice';
      // Execute
      middleware(Actions.deleteInvoice(invoiceID)).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          expect(dispatch.mock.calls.length).toBe(2);
          // Dispatch success notification
          expect(dispatch.mock.calls[0][0]).toEqual({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
          // Dispatch clear Form action
          expect(dispatch.mock.calls[1][0]).toEqual({
            type: ACTION_TYPES.FORM_CLEAR
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':471,'endLine':471}","it('update the current invoice and dispatch save action', () => {
      const invoiceData = {
        _id: 'a-random-string',
        _rev: 'another-random-string',
        created_at: 'yesterday',
      }
      // Action
      middleware(Actions.duplicateInvoice(invoiceData));
      // Expect
      expect(dispatch.mock.calls.length).toBe(1);
      expect(dispatch).toHaveBeenCalledWith({
        type: ACTION_TYPES.INVOICE_SAVE,
        payload: Object.assign({}, invoiceData, {
          _id: 'id-string',
          _rev: null,
          created_at: 'now',
        }),
      });
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':489,'endLine':489}","it('get the doc, merge with config object and dispatch a new action', () => {
      const invoiceID = 'id-string';
      const configs = { color: 'red' };
      middleware(Actions.saveInvoiceConfigs(invoiceID, configs)).then(() =>
        getSingleDoc('invoices', invoiceID).then(doc => {
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: Object.assign({}, doc, { configs }),
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':526,'endLine':526}","it('get the doc, merge with status object and dispatch a new action', () => {
      const invoiceID = 'id-string';
      const status = 'paid';
      middleware(Actions.setInvoiceStatus(invoiceID, status)).then(() =>
        getSingleDoc('invoices', invoiceID).then(doc => {
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: Object.assign({}, doc, { status }),
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':566,'endLine':566}","it('should handle INVOICE_NEW_FROM_CONTACT action', () => {
    const contact = {
      id: 'jon-snow',
      fullname: 'Jon Snow',
      email: 'jon@hbo.com',
    };
    const action = Actions.newInvoiceFromContact(contact);
    middleware(action);
    // Move to Form Tab
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith({
      type: ACTION_TYPES.UI_TAB_CHANGE,
      payload: 'form',
    });
    // Update Form recipient data
    expect(dispatch.mock.calls.length).toBe(1);
    expect(dispatch).toHaveBeenCalledWith({
      type: ACTION_TYPES.FORM_RECIPIENT_UPDATE,
      payload: {
        new: {},
        select: contact,
        newRecipient: false,
      },
    });
  })",snuts
/app/middlewares/__tests__/InvoicesMW.spec.js,SubOptimalAssert,"{'startLine':572,'endLine':572}","it('should handle INVOICE_NEW_FROM_CONTACT action', () => {
    const contact = {
      id: 'jon-snow',
      fullname: 'Jon Snow',
      email: 'jon@hbo.com',
    };
    const action = Actions.newInvoiceFromContact(contact);
    middleware(action);
    // Move to Form Tab
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith({
      type: ACTION_TYPES.UI_TAB_CHANGE,
      payload: 'form',
    });
    // Update Form recipient data
    expect(dispatch.mock.calls.length).toBe(1);
    expect(dispatch).toHaveBeenCalledWith({
      type: ACTION_TYPES.FORM_RECIPIENT_UPDATE,
      payload: {
        new: {},
        select: contact,
        newRecipient: false,
      },
    });
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':34,'endLine':34}","it('should save Invocie, save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: {
            newRecipient: true
          },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      // Action
      middleware(Actions.saveFormData());
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice, Save new contact & Clear the Form
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':36,'endLine':36}","it('should save Invocie, save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: {
            newRecipient: true
          },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      // Action
      middleware(Actions.saveFormData());
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice, Save new contact & Clear the Form
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':38,'endLine':38}","it('should save Invocie, save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: {
            newRecipient: true
          },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      // Action
      middleware(Actions.saveFormData());
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice, Save new contact & Clear the Form
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':61,'endLine':61}","it('should save Invocie, NOT save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice & Clear the Form
      expect(dispatch.mock.calls.length).toBe(2);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':63,'endLine':63}","it('should save Invocie, NOT save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice & Clear the Form
      expect(dispatch.mock.calls.length).toBe(2);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':65,'endLine':65}","it('should save Invocie, NOT save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice & Clear the Form
      expect(dispatch.mock.calls.length).toBe(2);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':87,'endLine':87}","it('should update Invocie, save Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: true },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      const action = Actions.saveFormData();
      // Action
      middleware(action);
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Save new contact, Clear the Form & Change Tab
      expect(dispatch.mock.calls.length).toBe(4);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':89,'endLine':89}","it('should update Invocie, save Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: true },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      const action = Actions.saveFormData();
      // Action
      middleware(action);
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Save new contact, Clear the Form & Change Tab
      expect(dispatch.mock.calls.length).toBe(4);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':91,'endLine':91}","it('should update Invocie, save Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: true },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      const action = Actions.saveFormData();
      // Action
      middleware(action);
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Save new contact, Clear the Form & Change Tab
      expect(dispatch.mock.calls.length).toBe(4);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':115,'endLine':115}","it('should update Invocie, NOT add new Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Clear the Form and Switch Tab
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':117,'endLine':117}","it('should update Invocie, NOT add new Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Clear the Form and Switch Tab
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':119,'endLine':119}","it('should update Invocie, NOT add new Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Clear the Form and Switch Tab
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':135,'endLine':135}","it('should NOT pass validation', () => {
    // Setup
    getState = jest.fn(() => ({
      form: { validation: false },
    }));
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.saveFormData();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(1);
    // No calling to Dispatch or Next since it failed validation
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(0);
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':137,'endLine':137}","it('should NOT pass validation', () => {
    // Setup
    getState = jest.fn(() => ({
      form: { validation: false },
    }));
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.saveFormData();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(1);
    // No calling to Dispatch or Next since it failed validation
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(0);
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':138,'endLine':138}","it('should NOT pass validation', () => {
    // Setup
    getState = jest.fn(() => ({
      form: { validation: false },
    }));
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.saveFormData();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(1);
    // No calling to Dispatch or Next since it failed validation
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(0);
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':151,'endLine':151}","it('should hanlde FORM_ITEM_ADD action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.addItem();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: Object.assign({}, action.payload, {
          id: 'id-string',
        }),
      })
    );
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':152,'endLine':152}","it('should hanlde FORM_ITEM_ADD action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.addItem();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: Object.assign({}, action.payload, {
          id: 'id-string',
        }),
      })
    );
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':172,'endLine':172}","it('should hanlde FORM_CLEAR action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.clearForm();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    // Close Form Settings & Add Item
    expect(dispatch.mock.calls.length).toBe(2);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(action);
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':174,'endLine':174}","it('should hanlde FORM_CLEAR action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.clearForm();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    // Close Form Settings & Add Item
    expect(dispatch.mock.calls.length).toBe(2);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(action);
  })",snuts
/app/middlewares/__tests__/FormMW.spec.js,SubOptimalAssert,"{'startLine':175,'endLine':175}","it('should hanlde FORM_CLEAR action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.clearForm();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    // Close Form Settings & Add Item
    expect(dispatch.mock.calls.length).toBe(2);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(action);
  })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,IdenticalTestDescription,"{'startLine':169,'endLine':189}","it('handle unknown error correctly', () => {
      // Setup
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const expectedError = new Error('Something Broken!');
      saveDoc.mockImplementationOnce(() => Promise.reject(expectedError));
      // Execute
      middleware(Actions.saveContact(newContact)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,IdenticalTestDescription,"{'startLine':205,'endLine':230}","it('should call next and dispatch notification ', () => {
      // Setup
      const contactID = 'jon-snow';
      const action = Actions.deleteContact(contactID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('contacts', contactID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:deleted')
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,IdenticalTestDescription,"{'startLine':232,'endLine':249}","it('handle error correctly', () => {
      // Set up
      const contactID = 'ned-stark';
      const expectedError = new Error('No contact found!');

      // Execute
      middleware(Actions.deleteContact(contactID)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,SubOptimalAssert,"{'startLine':43,'endLine':43}","it('calls next after the promised is returned', () => {
      const action = Actions.getAllContacts();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Calling next with resolved data
        return getAllDocs('contacts').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,SubOptimalAssert,"{'startLine':46,'endLine':46}","it('calls next after the promised is returned', () => {
      const action = Actions.getAllContacts();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Calling next with resolved data
        return getAllDocs('contacts').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,SubOptimalAssert,"{'startLine':134,'endLine':134}","it('should call next and dispatch notification ', () => {
      // Setup
      const newContact = {
        _id: 'id-string',
        created_at: 'now',
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const action = Actions.saveContact(newContact);

      // Execute
      middleware(action).then(() =>
        saveDoc('contacts', newContact).then(data => {
          // Expect calling next
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.contactsRecords,
                newContact
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:saved')
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,SubOptimalAssert,"{'startLine':144,'endLine':144}","it('should call next and dispatch notification ', () => {
      // Setup
      const newContact = {
        _id: 'id-string',
        created_at: 'now',
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const action = Actions.saveContact(newContact);

      // Execute
      middleware(action).then(() =>
        saveDoc('contacts', newContact).then(data => {
          // Expect calling next
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.contactsRecords,
                newContact
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:saved')
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,SubOptimalAssert,"{'startLine':213,'endLine':213}","it('should call next and dispatch notification ', () => {
      // Setup
      const contactID = 'jon-snow';
      const action = Actions.deleteContact(contactID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('contacts', contactID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:deleted')
            },
          });
        })
      );
    })",snuts
/app/middlewares/__tests__/ContactsMW.spec.js,SubOptimalAssert,"{'startLine':220,'endLine':220}","it('should call next and dispatch notification ', () => {
      // Setup
      const contactID = 'jon-snow';
      const action = Actions.deleteContact(contactID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('contacts', contactID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:deleted')
            },
          });
        })
      );
    })",snuts
/app/components/shared/_tests_/SplitButton.spec.js,AnonymousTest,"{'startLine':63,'endLine':68}","it('matches snapshot', () => {
    const tree = renderer
      .create(<SplitButton mainButton={mainButton} options={options} />)
      .toJSON();
    expect(tree).toMatchSnapshot();
  })",snuts
/app/components/shared/_tests_/SplitButton.spec.js,GeneralFixture,"{'startLine':74,'endLine':74}","beforeEach(() => {
    spyHandleClick = jest.spyOn(SplitButton.prototype, 'handleClick');
    spyHandleOutsideClick = jest.spyOn(
      SplitButton.prototype,
      'handleOutsideClick'
    );
    wrapper = shallow(
      <SplitButton mainButton={mainButton} options={options} />
    );
    mainBtn = wrapper.find('a').first();
    toggleBtn = wrapper.find('a').last();
  })",snuts
/app/components/shared/_tests_/SplitButton.spec.js,GeneralFixture,"{'startLine':75,'endLine':78}","beforeEach(() => {
    spyHandleClick = jest.spyOn(SplitButton.prototype, 'handleClick');
    spyHandleOutsideClick = jest.spyOn(
      SplitButton.prototype,
      'handleOutsideClick'
    );
    wrapper = shallow(
      <SplitButton mainButton={mainButton} options={options} />
    );
    mainBtn = wrapper.find('a').first();
    toggleBtn = wrapper.find('a').last();
  })",snuts
/app/components/shared/_tests_/Message.spec.js,IdenticalTestDescription,"{'startLine':18,'endLine':21}","it('renders a DIV element', () => {
    const message = mount(<Message text=""foo"" />);
    expect(message.find('div')).toHaveLength(1);
  })",snuts
/app/components/shared/_tests_/Layout.spec.js,IdenticalTestDescription,"{'startLine':85,'endLine':94}","it('PageHeaderTitle renders a DIV element', () => {
    const pageHeaderTitle = mount(
      <PageHeaderTitle>
        <span>Hello, World</span>
      </PageHeaderTitle>
    );
    expect(pageHeaderTitle.find('p')).toHaveLength(1);
    expect(pageHeaderTitle.find('span')).toHaveLength(1);
    expect(pageHeaderTitle.text()).toEqual('Hello, World');
  })",snuts
/app/components/invoices/__tests__/Invoice.spec.js,AnonymousTest,"{'startLine':198,'endLine':214}","it('matches snapshot', () => {
    const tree = renderer
      .create(
        <Invoice
          t={t}
          invoice={invoice}
          editInvoice={editInvoice}
          deleteInvoice={deleteInvoice}
          duplicateInvoice={duplicateInvoice}
          setInvoiceStatus={setInvoiceStatus}
          dateFormat={dateFormat}
          currencyPlacement={currencyPlacement}
        />
      )
      .toJSON();
    expect(tree).toMatchSnapshot();
  })",snuts
/app/components/invoices/__tests__/Invoice.spec.js,GeneralFixture,"{'startLine':229,'endLine':229}","beforeEach(() => {
    spyViewAction = jest.spyOn(Invoice.prototype, 'viewInvoice');
    spyEditAction = jest.spyOn(Invoice.prototype, 'editInvoice');
    spyDuplicateAction = jest.spyOn(Invoice.prototype, 'duplicateInvoice');
    wrapper = shallow(
      <Invoice
        t={t}
        invoice={invoice}
        editInvoice={editInvoice}
        deleteInvoice={deleteInvoice}
        duplicateInvoice={duplicateInvoice}
        setInvoiceStatus={setInvoiceStatus}
        dateFormat={dateFormat}
        currencyPlacement={currencyPlacement}
      />
    );
    viewBtn = wrapper
      .find('Invoice__Footer')
      .find(Button)
      .last();
    editBtn = wrapper
      .find('Invoice__Footer')
      .find(Button)
      .first();
    deleteBtn = wrapper.find(Button).at(1);
    duplicateBtn = wrapper.find(Button).first();
  })",snuts
/app/components/invoices/__tests__/Invoice.spec.js,IdenticalTestDescription,"{'startLine':277,'endLine':280}","it('handle edit action correctly', () => {
    editBtn.simulate('click');
    expect(spyEditAction).toHaveBeenCalled();
  })",snuts
/app/components/layout/__tests__/AppNav.spec.js,CommentsOnlyTest,"{'startLine':21,'endLine':24}","it('has correct porps', () => {
    // expect(wrapper.prop('activeTab')).toEqual('form');
    // expect(wrapper.prop('activeTab')).not.toEqual('settings');
  })",snuts
/app/components/layout/__tests__/AppNav.spec.js,CommentsOnlyTest,"{'startLine':26,'endLine':29}","it('contains active indicator', () => {
  //   const sideBar = shallow(<ActiveIndicator/>);
  //   expect(wrapper.contains(sideBar)).toEqual(true);
  })",snuts
/app/components/contacts/__tests__/Contact.spec.js,AnonymousTest,"{'startLine':98,'endLine':109}","it('matches snapshot', () => {
    const tree = renderer
      .create(
        <Contact
          contact={contact}
          newInvoice={newInvoice}
          deleteContact={deleteContact}
        />
      )
      .toJSON();
    expect(tree).toMatchSnapshot();
  })",snuts
/test/spec.js,IdenticalTestDescription,"{'startLine':99,'endLine':102}","it('with correct title', () =>
        app.client.browserWindow.getTitle().then(title => {
          expect(title).to.equal('Manta');
        }))",snuts
/test/spec.js,IdenticalTestDescription,"{'startLine':104,'endLine':121}","it('with correct properties', () =>
        app.client.browserWindow
          .isMinimized()
          .then(isMinimized => {
            expect(isMinimized).to.be.false;
          })
          .browserWindow.isResizable()
          .then(isResizable => {
            expect(isResizable).to.be.true;
          })
          .browserWindow.isMovable()
          .then(isMovable => {
            expect(isMovable).to.be.true;
          })
          .browserWindow.isDevToolsOpened()
          .then(isDevToolsOpened => {
            expect(isDevToolsOpened).to.be.false;
          }))",snuts
/test/spec.js,IdenticalTestDescription,"{'startLine':125,'endLine':134}","it('with correct visibility and focus', () =>
        app.client.browserWindow
          .isVisible()
          .then(isVisible => {
            expect(isVisible).to.be.true;
          })
          .browserWindow.isFocused()
          .then(isFocused => {
            expect(isFocused).to.be.true;
          }))",snuts
/test/spec.js,IdenticalTestDescription,"{'startLine':143,'endLine':146}","it('with correct title', () =>
        app.client.browserWindow.getTitle().then(title => {
          expect(title).to.equal('Previewer');
        }))",snuts
/test/spec.js,IdenticalTestDescription,"{'startLine':148,'endLine':165}","it('with correct properties', () =>
        app.client.browserWindow
          .isMinimized()
          .then(isMinimized => {
            expect(isMinimized).to.be.false;
          })
          .browserWindow.isResizable()
          .then(isResizable => {
            expect(isResizable).to.be.true;
          })
          .browserWindow.isMovable()
          .then(isMovable => {
            expect(isMovable).to.be.true;
          })
          .browserWindow.isDevToolsOpened()
          .then(isDevToolsOpened => {
            expect(isDevToolsOpened).to.be.false;
          }))",snuts
/test/spec.js,IdenticalTestDescription,"{'startLine':169,'endLine':178}","it('with correct visibility and focus', () =>
        app.client.browserWindow
          .isVisible()
          .then(isVisible => {
            expect(isVisible).to.be.false;
          })
          .browserWindow.isFocused()
          .then(isFocused => {
            expect(isFocused).to.be.false;
          }))",snuts
/test/spec.js,Conditional Test Logic,"{'line': 8, 'column': 4, 'index': 213}","describe('Application launch', () => {
  before(() => app.start());
  after(() => {
    if (app && app.isRunning()) {
      return app.stop();
    }
  });

  describe('start up', () => {
    let tourWindow, mainWindow, previewWindow;

    beforeEach(() =>
      app.client.windowHandles().then(handles => {
        tourWindow = handles.value[2]; // Tour
        mainWindow = handles.value[1]; // Manta
        previewWindow = handles.value[0]; // Previewer
      })
    );

    it('it should create 3 windows', () =>
      app.client
        .waitUntilWindowLoaded()
        .getWindowCount()
        .then(count => {
          expect(count).to.equal(3);
        }));

    // Tour Window
    describe('it should create tourWindow', () => {
      beforeEach(() => app.client.waitUntilWindowLoaded().window(tourWindow));

      it('with correct title', () =>
        app.client.browserWindow.getTitle().then(title => {
          expect(title).to.equal('Tour');
        }));

      it('with correct size and placed in the center of the primary screen', () => {
        let displayWidth, displayHeight;
        return app.client.electron.screen
          .getPrimaryDisplay()
          .then(res => {
            displayWidth = res.bounds.width;
            displayHeight = res.bounds.height;
          })
          .browserWindow.getBounds()
          .then(res => {
            // Check Size
            expect(res.width).to.equal(700);
            expect(res.height).to.equal(600);
            // Check Position
            const winX = (displayWidth - res.width) / 2;
            const winY = (displayHeight - res.height) / 2;
            expect(winX).to.equal(res.x);
            expect(winY).to.equal(res.y);
          });
      });

      it('with correct properties', () =>
        app.client.browserWindow
          .isMinimized()
          .then(isMinimized => {
            expect(isMinimized).to.be.false;
          })
          .browserWindow.isResizable()
          .then(isResizable => {
            expect(isResizable).to.be.false;
          })
          .browserWindow.isMovable()
          .then(isMovable => {
            expect(isMovable).to.be.false;
          })
          .browserWindow.isDevToolsOpened()
          .then(isDevToolsOpened => {
            expect(isDevToolsOpened).to.be.false;
          }));

      // TODO
      // Need to be able to retrive appConfig in order to test this
      it('with correct visibility and focus', () =>
        app.client.browserWindow
          .isVisible()
          .then(isVisible => {
            expect(isVisible).to.be.false;
          })
          .browserWindow.isFocused()
          .then(isFocused => {
            expect(isFocused).to.be.false;
          }));
    });

    // Main Window
    describe('it should create mainWindow', () => {
      beforeEach(() => app.client.waitUntilWindowLoaded().window(mainWindow));

      it('with correct title', () =>
        app.client.browserWindow.getTitle().then(title => {
          expect(title).to.equal('Manta');
        }));

      it('with correct properties', () =>
        app.client.browserWindow
          .isMinimized()
          .then(isMinimized => {
            expect(isMinimized).to.be.false;
          })
          .browserWindow.isResizable()
          .then(isResizable => {
            expect(isResizable).to.be.true;
          })
          .browserWindow.isMovable()
          .then(isMovable => {
            expect(isMovable).to.be.true;
          })
          .browserWindow.isDevToolsOpened()
          .then(isDevToolsOpened => {
            expect(isDevToolsOpened).to.be.false;
          }));

      // TODO
      // Need to be able to retrive appConfig in order to test this
      it('with correct visibility and focus', () =>
        app.client.browserWindow
          .isVisible()
          .then(isVisible => {
            expect(isVisible).to.be.true;
          })
          .browserWindow.isFocused()
          .then(isFocused => {
            expect(isFocused).to.be.true;
          }));
    });

    // Preview Window
    describe('it should create previewWindow', () => {
      beforeEach(() =>
        app.client.waitUntilWindowLoaded().window(previewWindow)
      );

      it('with correct title', () =>
        app.client.browserWindow.getTitle().then(title => {
          expect(title).to.equal('Previewer');
        }));

      it('with correct properties', () =>
        app.client.browserWindow
          .isMinimized()
          .then(isMinimized => {
            expect(isMinimized).to.be.false;
          })
          .browserWindow.isResizable()
          .then(isResizable => {
            expect(isResizable).to.be.true;
          })
          .browserWindow.isMovable()
          .then(isMovable => {
            expect(isMovable).to.be.true;
          })
          .browserWindow.isDevToolsOpened()
          .then(isDevToolsOpened => {
            expect(isDevToolsOpened).to.be.false;
          }));

      // TODO
      // Need to be able to retrive appConfig in order to test this
      it('with correct visibility and focus', () =>
        app.client.browserWindow
          .isVisible()
          .then(isVisible => {
            expect(isVisible).to.be.false;
          })
          .browserWindow.isFocused()
          .then(isFocused => {
            expect(isFocused).to.be.false;
          }));
    });
  });
})",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 15, 'column': 29, 'index': 340}","it('return correct formatted number with commaDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'commaDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'commaDot');
    expect(formattedNumber2).toEqual('1,000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'commaDot');
    expect(formattedNumber3).toEqual('1,999.99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 17, 'column': 29, 'index': 454}","it('return correct formatted number with commaDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'commaDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'commaDot');
    expect(formattedNumber2).toEqual('1,000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'commaDot');
    expect(formattedNumber3).toEqual('1,999.99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 19, 'column': 29, 'index': 571}","it('return correct formatted number with commaDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'commaDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'commaDot');
    expect(formattedNumber2).toEqual('1,000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'commaDot');
    expect(formattedNumber3).toEqual('1,999.99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 24, 'column': 29, 'index': 766}","it('return correct formatted number with dotComma settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'dotComma');
    expect(formattedNumber1).toEqual('99,00');
    const formattedNumber2 = formatNumber(number2, 2, 'dotComma');
    expect(formattedNumber2).toEqual('1.000,00');
    const formattedNumber3 = formatNumber(number3, 2, 'dotComma');
    expect(formattedNumber3).toEqual('1.999,99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 26, 'column': 29, 'index': 880}","it('return correct formatted number with dotComma settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'dotComma');
    expect(formattedNumber1).toEqual('99,00');
    const formattedNumber2 = formatNumber(number2, 2, 'dotComma');
    expect(formattedNumber2).toEqual('1.000,00');
    const formattedNumber3 = formatNumber(number3, 2, 'dotComma');
    expect(formattedNumber3).toEqual('1.999,99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 28, 'column': 29, 'index': 997}","it('return correct formatted number with dotComma settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'dotComma');
    expect(formattedNumber1).toEqual('99,00');
    const formattedNumber2 = formatNumber(number2, 2, 'dotComma');
    expect(formattedNumber2).toEqual('1.000,00');
    const formattedNumber3 = formatNumber(number3, 2, 'dotComma');
    expect(formattedNumber3).toEqual('1.999,99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 33, 'column': 29, 'index': 1192}","it('return correct formatted number with spaceDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'spaceDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'spaceDot');
    expect(formattedNumber2).toEqual('1 000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'spaceDot');
    expect(formattedNumber3).toEqual('1 999.99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 35, 'column': 29, 'index': 1306}","it('return correct formatted number with spaceDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'spaceDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'spaceDot');
    expect(formattedNumber2).toEqual('1 000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'spaceDot');
    expect(formattedNumber3).toEqual('1 999.99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Eager Test,"{'line': 37, 'column': 29, 'index': 1423}","it('return correct formatted number with spaceDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'spaceDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'spaceDot');
    expect(formattedNumber2).toEqual('1 000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'spaceDot');
    expect(formattedNumber3).toEqual('1 999.99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Lazy Test,"{'line': 15, 'column': 29, 'index': 340}","it('return correct formatted number with commaDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'commaDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'commaDot');
    expect(formattedNumber2).toEqual('1,000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'commaDot');
    expect(formattedNumber3).toEqual('1,999.99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Lazy Test,"{'line': 24, 'column': 29, 'index': 766}","it('return correct formatted number with dotComma settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'dotComma');
    expect(formattedNumber1).toEqual('99,00');
    const formattedNumber2 = formatNumber(number2, 2, 'dotComma');
    expect(formattedNumber2).toEqual('1.000,00');
    const formattedNumber3 = formatNumber(number3, 2, 'dotComma');
    expect(formattedNumber3).toEqual('1.999,99');
  })",steel
/helpers/__tests__/formatNumber.spec.js,Lazy Test,"{'line': 33, 'column': 29, 'index': 1192}","it('return correct formatted number with spaceDot settings', () => {
    const formattedNumber1 = formatNumber(number1, 2, 'spaceDot');
    expect(formattedNumber1).toEqual('99.00');
    const formattedNumber2 = formatNumber(number2, 2, 'spaceDot');
    expect(formattedNumber2).toEqual('1 000.00');
    const formattedNumber3 = formatNumber(number3, 2, 'spaceDot');
    expect(formattedNumber3).toEqual('1 999.99');
  })",steel
/app/reducers/__tests__/UIReducer.spec.js,Magic Number,"{'line': 41, 'column': 48, 'index': 1216}","it('should handle UI_NOTIFICATION_NEW action', () => {
    const newState = UIReducer(initialState, {
      type: ACTION_TYPES.UI_NOTIFICATION_NEW,
      payload: {
        type: 'warning',
        message: 'Missing Document',
      },
    });
    expect(newState.notifications).toHaveLength(1);
    expect(newState.notifications[0].type).toEqual('warning');
    expect(newState.notifications[0].message).toEqual('Missing Document');
  })",steel
/app/reducers/__tests__/UIReducer.spec.js,Magic Number,"{'line': 66, 'column': 48, 'index': 1913}","it('should handle UI_NOTIFICATION_REMOVE action', () => {
    const currentState = {
      activeTab: 'form',
      notifications: [
        {
          id: 'abc',
          type: 'warning',
          message: 'Missing Doc',
        },
        {
          id: 'xyz',
          type: 'success',
          message: 'Invoice Created Successfully',
        },
      ],
    };
    const newState = UIReducer(currentState, {
      type: ACTION_TYPES.UI_NOTIFICATION_REMOVE,
      payload: 'abc',
    });
    expect(newState.notifications).toHaveLength(1);
    expect(newState.notifications[0].id).toEqual('xyz');
    const newState2 = UIReducer(currentState, {
      type: ACTION_TYPES.UI_NOTIFICATION_REMOVE,
      payload: 'xyz',
    });
    expect(newState2.notifications).toHaveLength(1);
    expect(newState2.notifications[0].id).toEqual('abc');
  })",steel
/app/reducers/__tests__/UIReducer.spec.js,Magic Number,"{'line': 72, 'column': 49, 'index': 2150}","it('should handle UI_NOTIFICATION_REMOVE action', () => {
    const currentState = {
      activeTab: 'form',
      notifications: [
        {
          id: 'abc',
          type: 'warning',
          message: 'Missing Doc',
        },
        {
          id: 'xyz',
          type: 'success',
          message: 'Invoice Created Successfully',
        },
      ],
    };
    const newState = UIReducer(currentState, {
      type: ACTION_TYPES.UI_NOTIFICATION_REMOVE,
      payload: 'abc',
    });
    expect(newState.notifications).toHaveLength(1);
    expect(newState.notifications[0].id).toEqual('xyz');
    const newState2 = UIReducer(currentState, {
      type: ACTION_TYPES.UI_NOTIFICATION_REMOVE,
      payload: 'xyz',
    });
    expect(newState2.notifications).toHaveLength(1);
    expect(newState2.notifications[0].id).toEqual('abc');
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 90, 'column': 4, 'index': 2120}","it('drag row item', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_ITEM_MOVE,
      payload: {
        dragIndex: 2,
        hoverIndex: 3,
      },
    });
    expect(newState.rows).toHaveLength(4);
    expect(newState.rows).toEqual([
      { id: 'Jon Snow' },
      { id: 'Daenerys Targaryen' },
      { id: 'Arya Stark' },
      { id: 'Tyrion Lannister' },
    ]);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 91, 'column': 4, 'index': 2163}","it('drag row item', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_ITEM_MOVE,
      payload: {
        dragIndex: 2,
        hoverIndex: 3,
      },
    });
    expect(newState.rows).toHaveLength(4);
    expect(newState.rows).toEqual([
      { id: 'Jon Snow' },
      { id: 'Daenerys Targaryen' },
      { id: 'Arya Stark' },
      { id: 'Tyrion Lannister' },
    ]);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 240, 'column': 4, 'index': 6067}","it('recipient data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_RECIPIENT_UPDATE,
      payload: {
        newRecipient: false,
        select: {
          fullname: 'Jon Snow',
          company: 'HBO',
          email: 'jon@snow.got',
          phone: '000000000',
        },
        new: {},
      },
    });
    expect(newState.recipient.newRecipient).toBe(false);
    expect(newState.recipient.select).toEqual({
      fullname: 'Jon Snow',
      company: 'HBO',
      email: 'jon@snow.got',
      phone: '000000000',
    });
    expect(newState.recipient.select).not.toEqual({
      fullname: 'Dany',
      company: 'HBO',
      email: 'mother@dragons.got',
      phone: '111111111',
    });
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 246, 'column': 4, 'index': 6228}","it('recipient data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_RECIPIENT_UPDATE,
      payload: {
        newRecipient: false,
        select: {
          fullname: 'Jon Snow',
          company: 'HBO',
          email: 'jon@snow.got',
          phone: '000000000',
        },
        new: {},
      },
    });
    expect(newState.recipient.newRecipient).toBe(false);
    expect(newState.recipient.select).toEqual({
      fullname: 'Jon Snow',
      company: 'HBO',
      email: 'jon@snow.got',
      phone: '000000000',
    });
    expect(newState.recipient.select).not.toEqual({
      fullname: 'Dany',
      company: 'HBO',
      email: 'mother@dragons.got',
      phone: '111111111',
    });
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 272, 'column': 4, 'index': 6869}","it('currency data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'currency',
        data: {
          selectedCurrency: {
            symbol: '$',
            name: 'US Dollar',
            symbol_native: '$',
            decimal_digits: 2,
            rounding: 0,
            code: 'USD',
            name_plural: 'US dollars',
          },
        },
      },
    });
    expect(newState.currency.selectedCurrency.code).not.toEqual('VND');
    expect(newState.currency.selectedCurrency.code).toEqual('USD');
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 273, 'column': 4, 'index': 6941}","it('currency data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'currency',
        data: {
          selectedCurrency: {
            symbol: '$',
            name: 'US Dollar',
            symbol_native: '$',
            decimal_digits: 2,
            rounding: 0,
            code: 'USD',
            name_plural: 'US dollars',
          },
        },
      },
    });
    expect(newState.currency.selectedCurrency.code).not.toEqual('VND');
    expect(newState.currency.selectedCurrency.code).toEqual('USD');
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 300, 'column': 4, 'index': 7608}","it('discount data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'discount',
        data: {
          type: 'percentage',
          amount: 10,
        },
      },
    });
    expect(newState.discount.type).toEqual('percentage');
    expect(newState.discount.type).not.toEqual('flat');
    expect(newState.discount.amount).toEqual(10);
    expect(newState.discount.amount).not.toEqual(5);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 301, 'column': 4, 'index': 7666}","it('discount data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'discount',
        data: {
          type: 'percentage',
          amount: 10,
        },
      },
    });
    expect(newState.discount.type).toEqual('percentage');
    expect(newState.discount.type).not.toEqual('flat');
    expect(newState.discount.amount).toEqual(10);
    expect(newState.discount.amount).not.toEqual(5);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 302, 'column': 4, 'index': 7722}","it('discount data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'discount',
        data: {
          type: 'percentage',
          amount: 10,
        },
      },
    });
    expect(newState.discount.type).toEqual('percentage');
    expect(newState.discount.type).not.toEqual('flat');
    expect(newState.discount.amount).toEqual(10);
    expect(newState.discount.amount).not.toEqual(5);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 303, 'column': 4, 'index': 7772}","it('discount data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'discount',
        data: {
          type: 'percentage',
          amount: 10,
        },
      },
    });
    expect(newState.discount.type).toEqual('percentage');
    expect(newState.discount.type).not.toEqual('flat');
    expect(newState.discount.amount).toEqual(10);
    expect(newState.discount.amount).not.toEqual(5);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 316, 'column': 4, 'index': 8059}","it('tax data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'tax',
        data: {
          amount: 5,
        },
      },
    });
    expect(newState.tax.amount).toEqual(5);
    expect(newState.tax.amount).not.toEqual(10);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 317, 'column': 4, 'index': 8103}","it('tax data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'tax',
        data: {
          amount: 5,
        },
      },
    });
    expect(newState.tax.amount).toEqual(5);
    expect(newState.tax.amount).not.toEqual(10);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 330, 'column': 4, 'index': 8416}","it('note data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'note',
        data: {
          content: 'You know nothing, Jon Snow',
        },
      },
    });
    expect(newState.note.content).toEqual('You know nothing, Jon Snow');
    expect(newState.note.content).not.toEqual(
      'The Lannisters always pay their debts'
    );
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Duplicate Assert,"{'line': 331, 'column': 4, 'index': 8489}","it('note data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'note',
        data: {
          content: 'You know nothing, Jon Snow',
        },
      },
    });
    expect(newState.note.content).toEqual('You know nothing, Jon Snow');
    expect(newState.note.content).not.toEqual(
      'The Lannisters always pay their debts'
    );
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 58, 'column': 39, 'index': 1329}","it('adding a row item', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_ITEM_ADD,
    });
    expect(newState.rows).toHaveLength(5);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 66, 'column': 39, 'index': 1542}","it('remove a row item', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_ITEM_REMOVE,
      payload: 'Arya Stark',
    });
    expect(newState.rows).toHaveLength(3);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 90, 'column': 39, 'index': 2155}","it('drag row item', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_ITEM_MOVE,
      payload: {
        dragIndex: 2,
        hoverIndex: 3,
      },
    });
    expect(newState.rows).toHaveLength(4);
    expect(newState.rows).toEqual([
      { id: 'Jon Snow' },
      { id: 'Daenerys Targaryen' },
      { id: 'Arya Stark' },
      { id: 'Tyrion Lannister' },
    ]);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 108, 'column': 39, 'index': 2606}","it('clear the form', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_CLEAR,
    });
    expect(newState.recipient).toEqual({
      newRecipient: true,
      select: {},
      new: {},
    });
    expect(newState.rows).toHaveLength(0);
    expect(newState.dueDate).toEqual({
      selectedDate: null,
      paymentTerm: null,
      useCustom: true,
    });
    expect(newState.note).toEqual({});
    expect(newState.currency).toEqual({
      code: 'USD',
      fraction: 2,
      placement: 'before',
      separator: 'commaDot'
    });
    expect(newState.discount).toEqual({});
    expect(newState.tax).toEqual({});
    expect(newState.settings.open).toEqual(false);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 302, 'column': 45, 'index': 7763}","it('discount data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'discount',
        data: {
          type: 'percentage',
          amount: 10,
        },
      },
    });
    expect(newState.discount.type).toEqual('percentage');
    expect(newState.discount.type).not.toEqual('flat');
    expect(newState.discount.amount).toEqual(10);
    expect(newState.discount.amount).not.toEqual(5);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 303, 'column': 49, 'index': 7817}","it('discount data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'discount',
        data: {
          type: 'percentage',
          amount: 10,
        },
      },
    });
    expect(newState.discount.type).toEqual('percentage');
    expect(newState.discount.type).not.toEqual('flat');
    expect(newState.discount.amount).toEqual(10);
    expect(newState.discount.amount).not.toEqual(5);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 316, 'column': 40, 'index': 8095}","it('tax data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'tax',
        data: {
          amount: 5,
        },
      },
    });
    expect(newState.tax.amount).toEqual(5);
    expect(newState.tax.amount).not.toEqual(10);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 317, 'column': 44, 'index': 8143}","it('tax data', () => {
    const newState = FormReducer(currentState, {
      type: ACTION_TYPES.FORM_FIELD_UPDATE_DATA,
      payload: {
        field: 'tax',
        data: {
          amount: 5,
        },
      },
    });
    expect(newState.tax.amount).toEqual(5);
    expect(newState.tax.amount).not.toEqual(10);
  })",steel
/app/reducers/__tests__/FormReducer.spec.js,Magic Number,"{'line': 484, 'column': 41, 'index': 12427}","it('should populate other fields data correctly', () => {
    // Rows
    expect(newState.rows.length).toEqual(1);
    expect(newState.rows).toEqual(invoiceData.rows);
    // Tax
    expect(newState.tax).toEqual(invoiceData.tax);
    // Currency
    expect(newState.currency).toEqual(invoiceData.currency);
    // Note
    expect(newState.note.content).toEqual(invoiceData.note);
    // Discount
    expect(newState.discount).toEqual(invoiceData.discount);
    // DueDate
    expect(newState.dueDate).toEqual(invoiceData.dueDate);
  })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 25, 'column': 46, 'index': 731}","it('should NOT update active tab', () => {
      middleware(UIActions.changeActiveTab('form'));
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 26, 'column': 42, 'index': 777}","it('should NOT update active tab', () => {
      middleware(UIActions.changeActiveTab('form'));
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 32, 'column': 46, 'index': 965}","it('should update active tab', () => {
      const action = UIActions.changeActiveTab('settings');
      middleware(action);
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Test Sound
      expect(sounds.play).toBeCalledWith('TAP');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 33, 'column': 42, 'index': 1011}","it('should update active tab', () => {
      const action = UIActions.changeActiveTab('settings');
      middleware(action);
      expect(getState.mock.calls.length).toBe(1);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Test Sound
      expect(sounds.play).toBeCalledWith('TAP');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 47, 'column': 46, 'index': 1453}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti(
        'success',
        'Invoice has been created successfully!'
      );
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('SUCCESS');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 48, 'column': 42, 'index': 1499}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti(
        'success',
        'Invoice has been created successfully!'
      );
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('SUCCESS');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 64, 'column': 46, 'index': 2029}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti('warning', 'Missing Doc');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('WARNING');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 65, 'column': 42, 'index': 2075}","it('should handle UI_NOTIFICATION_NEW action', () => {
      const action = UIActions.newNoti('warning', 'Missing Doc');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      // Add id field with uuidv4 string to payload
      expect(next).toHaveBeenCalledWith(
        Object.assign({}, action, {
          payload: Object.assign({}, action.payload, {
            id: 'id-string',
          }),
        })
      );
      // Test Sound
      expect(sounds.play).toBeCalledWith('WARNING');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 83, 'column': 46, 'index': 2632}","it('should handle FORM_ITEM_ADD action', () => {
      const action = FormActions.addItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('ADD');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 84, 'column': 42, 'index': 2678}","it('should handle FORM_ITEM_ADD action', () => {
      const action = FormActions.addItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('ADD');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 92, 'column': 46, 'index': 2964}","it('should handle FORM_ITEM_REMOVE action', () => {
      const action = FormActions.removeItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('REMOVE');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 93, 'column': 42, 'index': 3010}","it('should handle FORM_ITEM_REMOVE action', () => {
      const action = FormActions.removeItem();
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      expect(sounds.play).toBeCalledWith('REMOVE');
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 101, 'column': 46, 'index': 3312}","it('should handle FORM_CLEAR action WIHOUT sound', () => {
      const action = FormActions.clearForm('muted');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
    })",steel
/app/middlewares/__tests__/UIMiddleware.spec.js,Magic Number,"{'line': 102, 'column': 42, 'index': 3358}","it('should handle FORM_CLEAR action WIHOUT sound', () => {
      const action = FormActions.clearForm('muted');
      middleware(action);
      expect(getState.mock.calls.length).toBe(0);
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
    })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Duplicate Assert,"{'line': 69, 'column': 6, 'index': 1868}","it('should validate currency and tax data', () => {
      middleware(action);
      expect(validateCurrency).toHaveBeenCalled();
      expect(validateCurrency).toHaveBeenCalledWith(true, action.payload.invoice.currency);
      expect(validateTax).toHaveBeenCalled();
      expect(validateTax).toHaveBeenCalledWith(true, action.payload.invoice.tax);
    })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Duplicate Assert,"{'line': 70, 'column': 6, 'index': 1919}","it('should validate currency and tax data', () => {
      middleware(action);
      expect(validateCurrency).toHaveBeenCalled();
      expect(validateCurrency).toHaveBeenCalledWith(true, action.payload.invoice.currency);
      expect(validateTax).toHaveBeenCalled();
      expect(validateTax).toHaveBeenCalledWith(true, action.payload.invoice.tax);
    })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Duplicate Assert,"{'line': 71, 'column': 6, 'index': 2011}","it('should validate currency and tax data', () => {
      middleware(action);
      expect(validateCurrency).toHaveBeenCalled();
      expect(validateCurrency).toHaveBeenCalledWith(true, action.payload.invoice.currency);
      expect(validateTax).toHaveBeenCalled();
      expect(validateTax).toHaveBeenCalledWith(true, action.payload.invoice.tax);
    })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Duplicate Assert,"{'line': 72, 'column': 6, 'index': 2057}","it('should validate currency and tax data', () => {
      middleware(action);
      expect(validateCurrency).toHaveBeenCalled();
      expect(validateCurrency).toHaveBeenCalledWith(true, action.payload.invoice.currency);
      expect(validateTax).toHaveBeenCalled();
      expect(validateTax).toHaveBeenCalledWith(true, action.payload.invoice.tax);
    })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Empty Test,"{'line': 76, 'column': 4, 'index': 2158}","it('should save data', () => {})",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Magic Number,"{'line': 31, 'column': 44, 'index': 999}","it('should handle SETTINGS_GET_INITIAL action', () => {
    const savedSettings = {
      profile: 'someSettings',
      invoice: 'someSettings',
      general: 'someSettings',
    };
    const action = Actions.getInitalSettings();
    middleware(action);
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: {
          current: savedSettings,
          saved: savedSettings,
        },
      })
    );
  })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Magic Number,"{'line': 32, 'column': 40, 'index': 1043}","it('should handle SETTINGS_GET_INITIAL action', () => {
    const savedSettings = {
      profile: 'someSettings',
      invoice: 'someSettings',
      general: 'someSettings',
    };
    const action = Actions.getInitalSettings();
    middleware(action);
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: {
          current: savedSettings,
          saved: savedSettings,
        },
      })
    );
  })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Magic Number,"{'line': 88, 'column': 49, 'index': 2570}","it('should notify previewWindow of language & profile change', () => {
      // Clear calls count
      ipc.send.mockClear();
      middleware(action);
      expect(ipc.send).toHaveBeenCalled();
      expect(ipc.send.mock.calls.length).toEqual(2);
      // Notify about profile change
      expect(ipc.send.mock.calls[0][0]).toBe('change-preview-window-profile');
      expect(ipc.send.mock.calls[0][1]).toBe('New Profile');
      // Notify about UI Lang change
      expect(ipc.send.mock.calls[1][0]).toBe('change-preview-window-language');
      expect(ipc.send.mock.calls[1][1]).toBe('fr');
    })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Magic Number,"{'line': 100, 'column': 42, 'index': 3077}","it('should call next and dispatch notification', () => {
      middleware(action);
      // Call Next
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Send Notification
      expect(dispatch.mock.calls.length).toBe(1);
      expect(dispatch).toHaveBeenCalledWith({
        type: ACTION_TYPES.UI_NOTIFICATION_NEW,
        payload: {
          type: 'success',
          message: i18n.t('messages:settings:saved'),
        },
      });
    })",steel
/app/middlewares/__tests__/SettingsMW.spec.js,Magic Number,"{'line': 103, 'column': 46, 'index': 3203}","it('should call next and dispatch notification', () => {
      middleware(action);
      // Call Next
      expect(next.mock.calls.length).toBe(1);
      expect(next).toHaveBeenCalledWith(action);
      // Send Notification
      expect(dispatch.mock.calls.length).toBe(1);
      expect(dispatch).toHaveBeenCalledWith({
        type: ACTION_TYPES.UI_NOTIFICATION_NEW,
        payload: {
          type: 'success',
          message: i18n.t('messages:settings:saved'),
        },
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 40, 'column': 6, 'index': 1101}","it('retrieves data from correct DB', () => {
      middleware(Actions.getInvoices());

      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('invoices')).resolves.toBe(mockData.invoicesRecords);
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 41, 'column': 6, 'index': 1146}","it('retrieves data from correct DB', () => {
      middleware(Actions.getInvoices());

      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('invoices')).resolves.toBe(mockData.invoicesRecords);
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 42, 'column': 6, 'index': 1205}","it('retrieves data from correct DB', () => {
      middleware(Actions.getInvoices());

      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('invoices')).resolves.toBe(mockData.invoicesRecords);
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 43, 'column': 6, 'index': 1268}","it('retrieves data from correct DB', () => {
      middleware(Actions.getInvoices());

      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('invoices')).resolves.toBe(mockData.invoicesRecords);
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 72, 'column': 8, 'index': 2345}","it('handle error correctly', () => {
      // Make sure getAllDocs will be called with incorrect dbName
      getAllDocs.mockImplementationOnce(() => getAllDocs('random-string'));
      const expectedError = new Error('Incorrect database!');
      // Execute
      middleware(Actions.getInvoices()).then(() => {
        // Calling next & send new notification
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 73, 'column': 8, 'index': 2386}","it('handle error correctly', () => {
      // Make sure getAllDocs will be called with incorrect dbName
      getAllDocs.mockImplementationOnce(() => getAllDocs('random-string'));
      const expectedError = new Error('Incorrect database!');
      // Execute
      middleware(Actions.getInvoices()).then(() => {
        // Calling next & send new notification
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 88, 'column': 10, 'index': 2890}","it('handle unknown error correctly', () => {
      const unknownError = new Error('Something broke');
      getAllDocs.mockImplementationOnce(() => Promise.reject(unknownError));
      middleware(Actions.getInvoices()).then(() =>
        getAllDocs().catch(err => {
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: unknownError.message,
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 89, 'column': 10, 'index': 2933}","it('handle unknown error correctly', () => {
      const unknownError = new Error('Something broke');
      getAllDocs.mockImplementationOnce(() => Promise.reject(unknownError));
      middleware(Actions.getInvoices()).then(() =>
        getAllDocs().catch(err => {
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: unknownError.message,
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 200, 'column': 10, 'index': 6123}","it('tell main process to open preview window with invoice data', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      middleware(Actions.saveInvoice(newInvoice)).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // ipc to main process
          expect(ipcRenderer.send).toHaveBeenCalled();
          expect(ipcRenderer.send).toHaveBeenCalledWith(
            'preview-invoice',
            newInvoice
          );
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 201, 'column': 10, 'index': 6178}","it('tell main process to open preview window with invoice data', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      middleware(Actions.saveInvoice(newInvoice)).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // ipc to main process
          expect(ipcRenderer.send).toHaveBeenCalled();
          expect(ipcRenderer.send).toHaveBeenCalledWith(
            'preview-invoice',
            newInvoice
          );
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 238, 'column': 8, 'index': 7286}","it('handle unknown error correctly', () => {
      // Setup
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
      };
      const expectedError = new Error('Something Broken!');
      saveDoc.mockImplementationOnce(() => Promise.reject(expectedError));

      // Execute
      middleware(Actions.saveInvoice(newInvoice)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 239, 'column': 8, 'index': 7327}","it('handle unknown error correctly', () => {
      // Setup
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
      };
      const expectedError = new Error('Something Broken!');
      saveDoc.mockImplementationOnce(() => Promise.reject(expectedError));

      // Execute
      middleware(Actions.saveInvoice(newInvoice)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 359, 'column': 8, 'index': 11082}","it('should handle unkown error correctly', () => {
      const expectedError = new Error('Something Broken!');
      updateDoc.mockImplementationOnce(() => Promise.reject(expectedError));
      middleware(Actions.updateInvoice(currentInvoice)).then(() => {
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 360, 'column': 8, 'index': 11123}","it('should handle unkown error correctly', () => {
      const expectedError = new Error('Something Broken!');
      updateDoc.mockImplementationOnce(() => Promise.reject(expectedError));
      middleware(Actions.updateInvoice(currentInvoice)).then(() => {
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 449, 'column': 8, 'index': 13932}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'ned-stark';
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.deleteInvoice(invoiceID)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 450, 'column': 8, 'index': 13973}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'ned-stark';
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.deleteInvoice(invoiceID)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 507, 'column': 10, 'index': 15865}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const configs = { color: 'red' };
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.saveInvoiceConfigs(invoiceID, configs)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 508, 'column': 10, 'index': 15908}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const configs = { color: 'red' };
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.saveInvoiceConfigs(invoiceID, configs)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 544, 'column': 10, 'index': 17109}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const status = 'paid';
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.setInvoiceStatus(invoiceID, status)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Duplicate Assert,"{'line': 545, 'column': 10, 'index': 17152}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const status = 'paid';
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.setInvoiceStatus(invoiceID, status)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Eager Test,"{'line': 219, 'column': 15, 'index': 6709}","it('handle syntax error correctly', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
      };
      middleware(Actions.saveInvoice(newInvoice)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(saveDoc()).rejects.toEqual(dbError);
        expect(saveDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Eager Test,"{'line': 220, 'column': 15, 'index': 6761}","it('handle syntax error correctly', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
      };
      middleware(Actions.saveInvoice(newInvoice)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(saveDoc()).rejects.toEqual(dbError);
        expect(saveDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Eager Test,"{'line': 350, 'column': 15, 'index': 10690}","it('should handle syntax error correctly', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(updateDoc()).rejects.toEqual(dbError);
        expect(updateDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Eager Test,"{'line': 351, 'column': 15, 'index': 10744}","it('should handle syntax error correctly', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(updateDoc()).rejects.toEqual(dbError);
        expect(updateDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 45, 'column': 13, 'index': 1358}","it('retrieves data from correct DB', () => {
      middleware(Actions.getInvoices());

      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('invoices')).resolves.toBe(mockData.invoicesRecords);
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 54, 'column': 15, 'index': 1712}","it('calls next after the promised is returned', () => {
      const action = Actions.getInvoices();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Call next with resolved data
        return getAllDocs('invoices').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 67, 'column': 46, 'index': 2127}","it('handle error correctly', () => {
      // Make sure getAllDocs will be called with incorrect dbName
      getAllDocs.mockImplementationOnce(() => getAllDocs('random-string'));
      const expectedError = new Error('Incorrect database!');
      // Execute
      middleware(Actions.getInvoices()).then(() => {
        // Calling next & send new notification
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 87, 'column': 8, 'index': 2852}","it('handle unknown error correctly', () => {
      const unknownError = new Error('Something broke');
      getAllDocs.mockImplementationOnce(() => Promise.reject(unknownError));
      middleware(Actions.getInvoices()).then(() =>
        getAllDocs().catch(err => {
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: unknownError.message,
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 120, 'column': 8, 'index': 3760}","it('should save records to DB', () => {
      // Setup
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      // Execute
      middleware(Actions.saveInvoice(newInvoice)).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // Expect
          expect(data).toEqual([...mockData.invoicesRecords, newInvoice]);
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 152, 'column': 8, 'index': 4625}","it('should call next and dispatch notification ', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };

      // Execute
      const action = Actions.saveInvoice(newInvoice);
      middleware(action).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.invoicesRecords,
                newInvoice
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:saved'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 198, 'column': 8, 'index': 6033}","it('tell main process to open preview window with invoice data', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      middleware(Actions.saveInvoice(newInvoice)).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // ipc to main process
          expect(ipcRenderer.send).toHaveBeenCalled();
          expect(ipcRenderer.send).toHaveBeenCalledWith(
            'preview-invoice',
            newInvoice
          );
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 219, 'column': 15, 'index': 6709}","it('handle syntax error correctly', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
      };
      middleware(Actions.saveInvoice(newInvoice)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(saveDoc()).rejects.toEqual(dbError);
        expect(saveDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 318, 'column': 8, 'index': 9484}","it('should update the invoice', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() =>
        updateDoc('invoices', currentInvoice).then(data => {
          expect(data).toEqual(mockData.invoicesRecords);
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 326, 'column': 8, 'index': 9760}","it('should call next and dispatch notification', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() =>
        updateDoc('invoices', currentInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: mockData.invoicesRecords,
          });
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:updated'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 350, 'column': 15, 'index': 10690}","it('should handle syntax error correctly', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(updateDoc()).rejects.toEqual(dbError);
        expect(updateDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 375, 'column': 8, 'index': 11576}","it('should remove record from DB correctly', () => {
      const invoiceID = 'jon-invoice';
      middleware(Actions.deleteInvoice(invoiceID)).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          // Correctly resolves
          expect(data).toEqual([]);
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 388, 'column': 8, 'index': 11953}","it('should call next and dispatch notification ', () => {
      // Setup
      const invoiceID = 'jon-invoice';
      const action = Actions.deleteInvoice(invoiceID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 424, 'column': 8, 'index': 13101}","it('should clear the form if this invoice is being editted', () => {
      const getState = jest.fn(() => ({
        form: {
          settings: {
            editMode: {
              active: true,
              data: { _id: 'jon-invoice' }
            }
          }
        }
      }));
      const middleware = InvoicesMW({ dispatch, getState })(next);
      const invoiceID = 'jon-invoice';
      // Execute
      middleware(Actions.deleteInvoice(invoiceID)).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          expect(dispatch.mock.calls.length).toBe(2);
          // Dispatch success notification
          expect(dispatch.mock.calls[0][0]).toEqual({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
          // Dispatch clear Form action
          expect(dispatch.mock.calls[1][0]).toEqual({
            type: ACTION_TYPES.FORM_CLEAR
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 488, 'column': 8, 'index': 15192}","it('get the doc, merge with config object and dispatch a new action', () => {
      const invoiceID = 'id-string';
      const configs = { color: 'red' };
      middleware(Actions.saveInvoiceConfigs(invoiceID, configs)).then(() =>
        getSingleDoc('invoices', invoiceID).then(doc => {
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: Object.assign({}, doc, { configs }),
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 505, 'column': 8, 'index': 15790}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const configs = { color: 'red' };
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.saveInvoiceConfigs(invoiceID, configs)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 525, 'column': 8, 'index': 16451}","it('get the doc, merge with status object and dispatch a new action', () => {
      const invoiceID = 'id-string';
      const status = 'paid';
      middleware(Actions.setInvoiceStatus(invoiceID, status)).then(() =>
        getSingleDoc('invoices', invoiceID).then(doc => {
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: Object.assign({}, doc, { status }),
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Lazy Test,"{'line': 542, 'column': 8, 'index': 17034}","it('handle error correctly', () => {
      // Setup
      const invoiceID = 'id-string';
      const status = 'paid';
      const expectedError = new Error('No invoice found!');
      // Execute
      middleware(Actions.setInvoiceStatus(invoiceID, status)).then(() => {
        getSingleDoc('test', invoiceID).then(() => {
          // Expect
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: expectedError.message,
            },
          });
        })
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 52, 'column': 48, 'index': 1653}","it('calls next after the promised is returned', () => {
      const action = Actions.getInvoices();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Call next with resolved data
        return getAllDocs('invoices').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 55, 'column': 46, 'index': 1796}","it('calls next after the promised is returned', () => {
      const action = Actions.getInvoices();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Call next with resolved data
        return getAllDocs('invoices').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 154, 'column': 46, 'index': 4772}","it('should call next and dispatch notification ', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };

      // Execute
      const action = Actions.saveInvoice(newInvoice);
      middleware(action).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.invoicesRecords,
                newInvoice
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:saved'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 164, 'column': 50, 'index': 5096}","it('should call next and dispatch notification ', () => {
      const newInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          placement: 'before',
          fraction: 2,
          separator: 'commaDot',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };

      // Execute
      const action = Actions.saveInvoice(newInvoice);
      middleware(action).then(() =>
        saveDoc('invoices', newInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.invoicesRecords,
                newInvoice
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:saved'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 275, 'column': 44, 'index': 8328}","it('should call next and dispatch change Tab action', () => {
      // Setup
      const currentInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          symbol: '$',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      // Execute
      const action = Actions.editInvoice(currentInvoice);
      middleware(action).then(() => {
        // Call next
        expect(next.mock.calls.length).toBe(1);
        expect(next).toHaveBeenCalledWith(
          Object.assign({}, action, {
            payload: Object.assign({}, action.payload, {
              contacts: mockData.contactsRecords
            })
          })
        );
        // Dispatch change Tab action
        expect(dispatch.mock.calls.length).toBe(1);
        expect(dispatch).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_TAB_CHANGE,
          payload: 'form'
        });
      })
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 284, 'column': 48, 'index': 8644}","it('should call next and dispatch change Tab action', () => {
      // Setup
      const currentInvoice = {
        recipient: {
          fullname: faker.name.findName(),
          email: faker.internet.email(),
        },
        currency: {
          code: 'USD',
          symbol: '$',
        },
        rows: [
          {
            id: uuidv4(),
            description: faker.commerce.productName(),
            price: faker.commerce.price(),
            quantity: faker.random.number(10),
          },
        ],
      };
      // Execute
      const action = Actions.editInvoice(currentInvoice);
      middleware(action).then(() => {
        // Call next
        expect(next.mock.calls.length).toBe(1);
        expect(next).toHaveBeenCalledWith(
          Object.assign({}, action, {
            payload: Object.assign({}, action.payload, {
              contacts: mockData.contactsRecords
            })
          })
        );
        // Dispatch change Tab action
        expect(dispatch.mock.calls.length).toBe(1);
        expect(dispatch).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_TAB_CHANGE,
          payload: 'form'
        });
      })
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 328, 'column': 46, 'index': 9913}","it('should call next and dispatch notification', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() =>
        updateDoc('invoices', currentInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: mockData.invoicesRecords,
          });
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:updated'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 334, 'column': 50, 'index': 10164}","it('should call next and dispatch notification', () => {
      middleware(Actions.updateInvoice(currentInvoice)).then(() =>
        updateDoc('invoices', currentInvoice).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: mockData.invoicesRecords,
          });
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:updated'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 390, 'column': 46, 'index': 12101}","it('should call next and dispatch notification ', () => {
      // Setup
      const invoiceID = 'jon-invoice';
      const action = Actions.deleteInvoice(invoiceID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 397, 'column': 50, 'index': 12338}","it('should call next and dispatch notification ', () => {
      // Setup
      const invoiceID = 'jon-invoice';
      const action = Actions.deleteInvoice(invoiceID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 425, 'column': 50, 'index': 13199}","it('should clear the form if this invoice is being editted', () => {
      const getState = jest.fn(() => ({
        form: {
          settings: {
            editMode: {
              active: true,
              data: { _id: 'jon-invoice' }
            }
          }
        }
      }));
      const middleware = InvoicesMW({ dispatch, getState })(next);
      const invoiceID = 'jon-invoice';
      // Execute
      middleware(Actions.deleteInvoice(invoiceID)).then(() =>
        deleteDoc('invoices', invoiceID).then(data => {
          expect(dispatch.mock.calls.length).toBe(2);
          // Dispatch success notification
          expect(dispatch.mock.calls[0][0]).toEqual({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:invoice:deleted'),
            },
          });
          // Dispatch clear Form action
          expect(dispatch.mock.calls[1][0]).toEqual({
            type: ACTION_TYPES.FORM_CLEAR
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 471, 'column': 46, 'index': 14619}","it('update the current invoice and dispatch save action', () => {
      const invoiceData = {
        _id: 'a-random-string',
        _rev: 'another-random-string',
        created_at: 'yesterday',
      }
      // Action
      middleware(Actions.duplicateInvoice(invoiceData));
      // Expect
      expect(dispatch.mock.calls.length).toBe(1);
      expect(dispatch).toHaveBeenCalledWith({
        type: ACTION_TYPES.INVOICE_SAVE,
        payload: Object.assign({}, invoiceData, {
          _id: 'id-string',
          _rev: null,
          created_at: 'now',
        }),
      });
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 489, 'column': 50, 'index': 15292}","it('get the doc, merge with config object and dispatch a new action', () => {
      const invoiceID = 'id-string';
      const configs = { color: 'red' };
      middleware(Actions.saveInvoiceConfigs(invoiceID, configs)).then(() =>
        getSingleDoc('invoices', invoiceID).then(doc => {
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: Object.assign({}, doc, { configs }),
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 526, 'column': 50, 'index': 16551}","it('get the doc, merge with status object and dispatch a new action', () => {
      const invoiceID = 'id-string';
      const status = 'paid';
      middleware(Actions.setInvoiceStatus(invoiceID, status)).then(() =>
        getSingleDoc('invoices', invoiceID).then(doc => {
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.INVOICE_UPDATE,
            payload: Object.assign({}, doc, { status }),
          });
        })
      );
    })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 566, 'column': 40, 'index': 17721}","it('should handle INVOICE_NEW_FROM_CONTACT action', () => {
    const contact = {
      id: 'jon-snow',
      fullname: 'Jon Snow',
      email: 'jon@hbo.com',
    };
    const action = Actions.newInvoiceFromContact(contact);
    middleware(action);
    // Move to Form Tab
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith({
      type: ACTION_TYPES.UI_TAB_CHANGE,
      payload: 'form',
    });
    // Update Form recipient data
    expect(dispatch.mock.calls.length).toBe(1);
    expect(dispatch).toHaveBeenCalledWith({
      type: ACTION_TYPES.FORM_RECIPIENT_UPDATE,
      payload: {
        new: {},
        select: contact,
        newRecipient: false,
      },
    });
  })",steel
/app/middlewares/__tests__/InvoicesMW.spec.js,Magic Number,"{'line': 572, 'column': 44, 'index': 17914}","it('should handle INVOICE_NEW_FROM_CONTACT action', () => {
    const contact = {
      id: 'jon-snow',
      fullname: 'Jon Snow',
      email: 'jon@hbo.com',
    };
    const action = Actions.newInvoiceFromContact(contact);
    middleware(action);
    // Move to Form Tab
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith({
      type: ACTION_TYPES.UI_TAB_CHANGE,
      payload: 'form',
    });
    // Update Form recipient data
    expect(dispatch.mock.calls.length).toBe(1);
    expect(dispatch).toHaveBeenCalledWith({
      type: ACTION_TYPES.FORM_RECIPIENT_UPDATE,
      payload: {
        new: {},
        select: contact,
        newRecipient: false,
      },
    });
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 34, 'column': 46, 'index': 849}","it('should save Invocie, save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: {
            newRecipient: true
          },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      // Action
      middleware(Actions.saveFormData());
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice, Save new contact & Clear the Form
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 36, 'column': 46, 'index': 960}","it('should save Invocie, save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: {
            newRecipient: true
          },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      // Action
      middleware(Actions.saveFormData());
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice, Save new contact & Clear the Form
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 38, 'column': 42, 'index': 1031}","it('should save Invocie, save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: {
            newRecipient: true
          },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      // Action
      middleware(Actions.saveFormData());
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice, Save new contact & Clear the Form
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 61, 'column': 46, 'index': 1595}","it('should save Invocie, NOT save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice & Clear the Form
      expect(dispatch.mock.calls.length).toBe(2);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 63, 'column': 46, 'index': 1688}","it('should save Invocie, NOT save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice & Clear the Form
      expect(dispatch.mock.calls.length).toBe(2);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 65, 'column': 42, 'index': 1759}","it('should save Invocie, NOT save Contact and Clear the Form', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: false,
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Save the Invoice & Clear the Form
      expect(dispatch.mock.calls.length).toBe(2);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 87, 'column': 46, 'index': 2373}","it('should update Invocie, save Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: true },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      const action = Actions.saveFormData();
      // Action
      middleware(action);
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Save new contact, Clear the Form & Change Tab
      expect(dispatch.mock.calls.length).toBe(4);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 89, 'column': 46, 'index': 2498}","it('should update Invocie, save Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: true },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      const action = Actions.saveFormData();
      // Action
      middleware(action);
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Save new contact, Clear the Form & Change Tab
      expect(dispatch.mock.calls.length).toBe(4);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 91, 'column': 42, 'index': 2569}","it('should update Invocie, save Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: true },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);
      const action = Actions.saveFormData();
      // Action
      middleware(action);
      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Save new contact, Clear the Form & Change Tab
      expect(dispatch.mock.calls.length).toBe(4);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 115, 'column': 46, 'index': 3193}","it('should update Invocie, NOT add new Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Clear the Form and Switch Tab
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 117, 'column': 46, 'index': 3302}","it('should update Invocie, NOT add new Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Clear the Form and Switch Tab
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 119, 'column': 42, 'index': 3373}","it('should update Invocie, NOT add new Contact, Clear the Form and Switch Tab', () => {
      // Setup
      getState = jest.fn(() => ({
        form: {
          validation: true,
          recipient: { newRecipient: false },
          settings: {
            editMode: {
              active: true,
              data: { _id: 'invoice-uuid' }
            }
          }
        },
      }));
      const middleware = FormMW({ dispatch, getState })(next);

      // Action
      const action = Actions.saveFormData();
      middleware(action);

      // Expect
      expect(getState.mock.calls.length).toBe(1);
      // Update the Invoice, Clear the Form and Switch Tab
      expect(dispatch.mock.calls.length).toBe(3);
      // No Calling Next
      expect(next.mock.calls.length).toBe(0);
    })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 135, 'column': 44, 'index': 3726}","it('should NOT pass validation', () => {
    // Setup
    getState = jest.fn(() => ({
      form: { validation: false },
    }));
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.saveFormData();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(1);
    // No calling to Dispatch or Next since it failed validation
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(0);
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 137, 'column': 44, 'index': 3839}","it('should NOT pass validation', () => {
    // Setup
    getState = jest.fn(() => ({
      form: { validation: false },
    }));
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.saveFormData();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(1);
    // No calling to Dispatch or Next since it failed validation
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(0);
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 138, 'column': 40, 'index': 3883}","it('should NOT pass validation', () => {
    // Setup
    getState = jest.fn(() => ({
      form: { validation: false },
    }));
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.saveFormData();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(1);
    // No calling to Dispatch or Next since it failed validation
    expect(dispatch.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(0);
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 151, 'column': 44, 'index': 4181}","it('should hanlde FORM_ITEM_ADD action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.addItem();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: Object.assign({}, action.payload, {
          id: 'id-string',
        }),
      })
    );
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 152, 'column': 40, 'index': 4225}","it('should hanlde FORM_ITEM_ADD action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.addItem();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(
      Object.assign({}, action, {
        payload: Object.assign({}, action.payload, {
          id: 'id-string',
        }),
      })
    );
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 172, 'column': 44, 'index': 4703}","it('should hanlde FORM_CLEAR action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.clearForm();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    // Close Form Settings & Add Item
    expect(dispatch.mock.calls.length).toBe(2);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(action);
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 174, 'column': 44, 'index': 4789}","it('should hanlde FORM_CLEAR action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.clearForm();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    // Close Form Settings & Add Item
    expect(dispatch.mock.calls.length).toBe(2);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(action);
  })",steel
/app/middlewares/__tests__/FormMW.spec.js,Magic Number,"{'line': 175, 'column': 40, 'index': 4833}","it('should hanlde FORM_CLEAR action', () => {
    // Setup
    getState = jest.fn();
    const middleware = FormMW({ dispatch, getState })(next);

    // Action
    const action = Actions.clearForm();
    middleware(action);

    // Expect
    expect(getState.mock.calls.length).toBe(0);
    // Close Form Settings & Add Item
    expect(dispatch.mock.calls.length).toBe(2);
    expect(next.mock.calls.length).toBe(1);
    expect(next).toHaveBeenCalledWith(action);
  })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 31, 'column': 6, 'index': 841}","it('retrieves data correct DB', () => {
      // Execute
      middleware(Actions.getAllContacts());
      // Expect
      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('contacts')).resolves.toBe(mockData.contactsRecords);
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 32, 'column': 6, 'index': 886}","it('retrieves data correct DB', () => {
      // Execute
      middleware(Actions.getAllContacts());
      // Expect
      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('contacts')).resolves.toBe(mockData.contactsRecords);
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 33, 'column': 6, 'index': 945}","it('retrieves data correct DB', () => {
      // Execute
      middleware(Actions.getAllContacts());
      // Expect
      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('contacts')).resolves.toBe(mockData.contactsRecords);
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 34, 'column': 6, 'index': 1008}","it('retrieves data correct DB', () => {
      // Execute
      middleware(Actions.getAllContacts());
      // Expect
      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('contacts')).resolves.toBe(mockData.contactsRecords);
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 62, 'column': 8, 'index': 2046}","it('handle error correctly', () => {
      // Make sure getAllDocs will be called with incorrect dbName
      getAllDocs.mockImplementationOnce(() => getAllDocs('random-string'));
      const expectedError = new Error('Incorrect database!');
      // Execute
      middleware(Actions.getAllContacts()).then(() => {
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 63, 'column': 8, 'index': 2087}","it('handle error correctly', () => {
      // Make sure getAllDocs will be called with incorrect dbName
      getAllDocs.mockImplementationOnce(() => getAllDocs('random-string'));
      const expectedError = new Error('Incorrect database!');
      // Execute
      middleware(Actions.getAllContacts()).then(() => {
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 78, 'column': 10, 'index': 2594}","it('handle unknown error correctly', () => {
      const unknownError = new Error('Something broke');
      getAllDocs.mockImplementationOnce(() => Promise.reject(unknownError));
      middleware(Actions.getAllContacts()).then(() =>
        getAllDocs().catch(err => {
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: unknownError.message,
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 79, 'column': 10, 'index': 2637}","it('handle unknown error correctly', () => {
      const unknownError = new Error('Something broke');
      getAllDocs.mockImplementationOnce(() => Promise.reject(unknownError));
      middleware(Actions.getAllContacts()).then(() =>
        getAllDocs().catch(err => {
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: unknownError.message,
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 101, 'column': 6, 'index': 3222}","it('addes id and timestamp to contact data', () => {
      // Setup
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      // Execute
      middleware(Actions.saveContact(newContact));
      // Expect
      expect(saveDoc).toHaveBeenCalled();
      expect(saveDoc).toHaveBeenCalledWith('contacts', newContact);
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 102, 'column': 6, 'index': 3264}","it('addes id and timestamp to contact data', () => {
      // Setup
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      // Execute
      middleware(Actions.saveContact(newContact));
      // Expect
      expect(saveDoc).toHaveBeenCalled();
      expect(saveDoc).toHaveBeenCalledWith('contacts', newContact);
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 180, 'column': 8, 'index': 5745}","it('handle unknown error correctly', () => {
      // Setup
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const expectedError = new Error('Something Broken!');
      saveDoc.mockImplementationOnce(() => Promise.reject(expectedError));
      // Execute
      middleware(Actions.saveContact(newContact)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 181, 'column': 8, 'index': 5786}","it('handle unknown error correctly', () => {
      // Setup
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const expectedError = new Error('Something Broken!');
      saveDoc.mockImplementationOnce(() => Promise.reject(expectedError));
      // Execute
      middleware(Actions.saveContact(newContact)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 240, 'column': 8, 'index': 7563}","it('handle error correctly', () => {
      // Set up
      const contactID = 'ned-stark';
      const expectedError = new Error('No contact found!');

      // Execute
      middleware(Actions.deleteContact(contactID)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Duplicate Assert,"{'line': 241, 'column': 8, 'index': 7604}","it('handle error correctly', () => {
      // Set up
      const contactID = 'ned-stark';
      const expectedError = new Error('No contact found!');

      // Execute
      middleware(Actions.deleteContact(contactID)).then(() => {
        // Expect
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Eager Test,"{'line': 164, 'column': 15, 'index': 5205}","it('handle syntax error correctly', () => {
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      middleware(Actions.saveContact(newContact)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(saveDoc()).rejects.toEqual(dbError);
        expect(saveDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Eager Test,"{'line': 165, 'column': 15, 'index': 5257}","it('handle syntax error correctly', () => {
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      middleware(Actions.saveContact(newContact)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(saveDoc()).rejects.toEqual(dbError);
        expect(saveDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 36, 'column': 13, 'index': 1098}","it('retrieves data correct DB', () => {
      // Execute
      middleware(Actions.getAllContacts());
      // Expect
      // Call correct function
      expect(getAllDocs).toHaveBeenCalled();
      expect(getAllDocs).toHaveBeenCalledWith('contacts');
      expect(getAllDocs).not.toHaveBeenCalledWith('invoices');
      expect(getAllDocs).not.toHaveBeenCalledWith('');
      // Correctly resolves
      expect(getAllDocs('contacts')).resolves.toBe(mockData.contactsRecords);
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 45, 'column': 15, 'index': 1458}","it('calls next after the promised is returned', () => {
      const action = Actions.getAllContacts();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Calling next with resolved data
        return getAllDocs('contacts').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 58, 'column': 46, 'index': 1873}","it('handle error correctly', () => {
      // Make sure getAllDocs will be called with incorrect dbName
      getAllDocs.mockImplementationOnce(() => getAllDocs('random-string'));
      const expectedError = new Error('Incorrect database!');
      // Execute
      middleware(Actions.getAllContacts()).then(() => {
        expect(next).toHaveBeenCalled();
        expect(next).toHaveBeenCalledWith({
          type: ACTION_TYPES.UI_NOTIFICATION_NEW,
          payload: {
            type: 'warning',
            message: expectedError.message,
          },
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 77, 'column': 8, 'index': 2556}","it('handle unknown error correctly', () => {
      const unknownError = new Error('Something broke');
      getAllDocs.mockImplementationOnce(() => Promise.reject(unknownError));
      middleware(Actions.getAllContacts()).then(() =>
        getAllDocs().catch(err => {
          expect(next).toHaveBeenCalled();
          expect(next).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'warning',
              message: unknownError.message,
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 113, 'column': 8, 'index': 3596}","it('should save records to DB', () => {
      // Setup
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      // Execute
      middleware(Actions.saveContact(newContact)).then(() =>
        saveDoc('contacts', newContact).then(data => {
          // Expect
          expect(data).toEqual([...mockData.contactsRecords, newContact]);
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 132, 'column': 8, 'index': 4129}","it('should call next and dispatch notification ', () => {
      // Setup
      const newContact = {
        _id: 'id-string',
        created_at: 'now',
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const action = Actions.saveContact(newContact);

      // Execute
      middleware(action).then(() =>
        saveDoc('contacts', newContact).then(data => {
          // Expect calling next
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.contactsRecords,
                newContact
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:saved')
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 164, 'column': 15, 'index': 5205}","it('handle syntax error correctly', () => {
      const newContact = {
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      middleware(Actions.saveContact(newContact)).then(() => {
        const dbError = new Error('No database found!');
        const docError = new Error('No doc found!');
        expect(saveDoc()).rejects.toEqual(dbError);
        expect(saveDoc('invoices')).rejects.toEqual(docError);
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 199, 'column': 8, 'index': 6298}","it('should remove record from DB correctly', () => {
      // Setup
      const contactID = 'jon-snow';
      const action = Actions.deleteContact(contactID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('contacts', contactID).then(data => {
          expect(data).toEqual([]);
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Lazy Test,"{'line': 211, 'column': 8, 'index': 6640}","it('should call next and dispatch notification ', () => {
      // Setup
      const contactID = 'jon-snow';
      const action = Actions.deleteContact(contactID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('contacts', contactID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:deleted')
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Magic Number,"{'line': 43, 'column': 48, 'index': 1396}","it('calls next after the promised is returned', () => {
      const action = Actions.getAllContacts();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Calling next with resolved data
        return getAllDocs('contacts').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Magic Number,"{'line': 46, 'column': 46, 'index': 1542}","it('calls next after the promised is returned', () => {
      const action = Actions.getAllContacts();
      middleware(action).then(() => {
        // Never call dispatch
        expect(dispatch.mock.calls.length).toBe(0);
        // Calling next with resolved data
        return getAllDocs('contacts').then(data => {
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: data,
            })
          );
        });
      });
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Magic Number,"{'line': 134, 'column': 46, 'index': 4255}","it('should call next and dispatch notification ', () => {
      // Setup
      const newContact = {
        _id: 'id-string',
        created_at: 'now',
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const action = Actions.saveContact(newContact);

      // Execute
      middleware(action).then(() =>
        saveDoc('contacts', newContact).then(data => {
          // Expect calling next
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.contactsRecords,
                newContact
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:saved')
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Magic Number,"{'line': 144, 'column': 50, 'index': 4579}","it('should call next and dispatch notification ', () => {
      // Setup
      const newContact = {
        _id: 'id-string',
        created_at: 'now',
        fullname: faker.name.findName(),
        email: faker.internet.email(),
      };
      const action = Actions.saveContact(newContact);

      // Execute
      middleware(action).then(() =>
        saveDoc('contacts', newContact).then(data => {
          // Expect calling next
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [
                ...mockData.contactsRecords,
                newContact
              ],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:saved')
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Magic Number,"{'line': 213, 'column': 46, 'index': 6788}","it('should call next and dispatch notification ', () => {
      // Setup
      const contactID = 'jon-snow';
      const action = Actions.deleteContact(contactID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('contacts', contactID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:deleted')
            },
          });
        })
      );
    })",steel
/app/middlewares/__tests__/ContactsMW.spec.js,Magic Number,"{'line': 220, 'column': 50, 'index': 7025}","it('should call next and dispatch notification ', () => {
      // Setup
      const contactID = 'jon-snow';
      const action = Actions.deleteContact(contactID);
      // Execute
      middleware(action).then(() =>
        deleteDoc('contacts', contactID).then(data => {
          // Call next after the promised is returned
          expect(next.mock.calls.length).toBe(1);
          expect(next).toHaveBeenCalledWith(
            Object.assign({}, action, {
              payload: [],
            })
          );
          // Dispatch success notification
          expect(dispatch.mock.calls.length).toBe(1);
          expect(dispatch).toHaveBeenCalledWith({
            type: ACTION_TYPES.UI_NOTIFICATION_NEW,
            payload: {
              type: 'success',
              message: i18n.t('messages:contact:deleted')
            },
          });
        })
      );
    })",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 42, 'column': 2, 'index': 650}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 43, 'column': 2, 'index': 706}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 44, 'column': 2, 'index': 766}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 45, 'column': 2, 'index': 822}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 46, 'column': 2, 'index': 882}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 47, 'column': 2, 'index': 938}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 51, 'column': 2, 'index': 1048}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 52, 'column': 2, 'index': 1104}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 53, 'column': 2, 'index': 1164}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 54, 'column': 2, 'index': 1219}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 59, 'column': 2, 'index': 1391}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 60, 'column': 2, 'index': 1447}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 61, 'column': 2, 'index': 1507}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 62, 'column': 2, 'index': 1564}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 67, 'column': 2, 'index': 1744}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 68, 'column': 2, 'index': 1802}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 69, 'column': 2, 'index': 1864}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 70, 'column': 2, 'index': 1922}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 71, 'column': 2, 'index': 1986}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Duplicate Assert,"{'line': 72, 'column': 2, 'index': 2044}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 42, 'column': 9, 'index': 657}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 43, 'column': 9, 'index': 713}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 44, 'column': 9, 'index': 773}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 45, 'column': 9, 'index': 829}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 46, 'column': 9, 'index': 889}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 47, 'column': 9, 'index': 945}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 51, 'column': 9, 'index': 1055}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 52, 'column': 9, 'index': 1111}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 53, 'column': 9, 'index': 1171}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 54, 'column': 9, 'index': 1226}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 55, 'column': 9, 'index': 1285}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 59, 'column': 9, 'index': 1398}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 60, 'column': 9, 'index': 1454}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 61, 'column': 9, 'index': 1514}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 62, 'column': 9, 'index': 1571}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 63, 'column': 9, 'index': 1630}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 67, 'column': 9, 'index': 1751}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 68, 'column': 9, 'index': 1809}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 69, 'column': 9, 'index': 1871}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 70, 'column': 9, 'index': 1929}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 71, 'column': 9, 'index': 1993}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Eager Test,"{'line': 72, 'column': 9, 'index': 2051}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Lazy Test,"{'line': 42, 'column': 9, 'index': 657}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Lazy Test,"{'line': 51, 'column': 9, 'index': 1055}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Lazy Test,"{'line': 59, 'column': 9, 'index': 1398}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Lazy Test,"{'line': 67, 'column': 9, 'index': 1751}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 42, 'column': 50, 'index': 698}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 43, 'column': 54, 'index': 758}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 44, 'column': 50, 'index': 814}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 45, 'column': 54, 'index': 874}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 46, 'column': 50, 'index': 930}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 47, 'column': 54, 'index': 990}","it('should return correct subtotal value', () => {
  expect(getInvoiceValue(data1).subtotal).toEqual(200);
  expect(getInvoiceValue(data1).subtotal).not.toEqual(100);
  expect(getInvoiceValue(data2).subtotal).toEqual(160);
  expect(getInvoiceValue(data2).subtotal).not.toEqual(180);
  expect(getInvoiceValue(data3).subtotal).toEqual(300);
  expect(getInvoiceValue(data3).subtotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 51, 'column': 50, 'index': 1096}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 52, 'column': 54, 'index': 1156}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 53, 'column': 50, 'index': 1212}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 54, 'column': 54, 'index': 1271}","it('should return correct discount', () => {
  expect(getInvoiceValue(data1).discount).toEqual(100);
  expect(getInvoiceValue(data1).discount).not.toEqual(200);
  expect(getInvoiceValue(data2).discount).toEqual(16);
  expect(getInvoiceValue(data2).discount).not.toEqual(18);
  expect(getInvoiceValue(data3).discount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 59, 'column': 51, 'index': 1440}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 60, 'column': 55, 'index': 1500}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 61, 'column': 51, 'index': 1556}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 62, 'column': 55, 'index': 1617}","it('should return correct tax value', () => {
  expect(getInvoiceValue(data1).taxAmount).toEqual(10);
  expect(getInvoiceValue(data1).taxAmount).not.toEqual(11);
  expect(getInvoiceValue(data2).taxAmount).toEqual(7.2);
  expect(getInvoiceValue(data2).taxAmount).not.toEqual(8);
  expect(getInvoiceValue(data3).taxAmount).toEqual(undefined);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 67, 'column': 52, 'index': 1794}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 68, 'column': 56, 'index': 1856}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 69, 'column': 52, 'index': 1914}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 70, 'column': 56, 'index': 1976}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 71, 'column': 52, 'index': 2036}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/invoice.spec.js,Magic Number,"{'line': 72, 'column': 56, 'index': 2098}","it('should return correct grandTotal value', () => {
  expect(getInvoiceValue(data1).grandTotal).toEqual(110);
  expect(getInvoiceValue(data1).grandTotal).not.toEqual(100);
  expect(getInvoiceValue(data2).grandTotal).toEqual(144);
  expect(getInvoiceValue(data2).grandTotal).not.toEqual(151.2);
  expect(getInvoiceValue(data3).grandTotal).toEqual(300);
  expect(getInvoiceValue(data3).grandTotal).not.toEqual(310);
})",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 109, 'column': 4, 'index': 2494}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 111, 'column': 4, 'index': 2588}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 112, 'column': 4, 'index': 2636}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 113, 'column': 4, 'index': 2689}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 115, 'column': 4, 'index': 2778}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 116, 'column': 4, 'index': 2833}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 117, 'column': 4, 'index': 2889}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 118, 'column': 4, 'index': 2940}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 119, 'column': 4, 'index': 2992}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 120, 'column': 4, 'index': 3048}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 131, 'column': 4, 'index': 3395}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 132, 'column': 4, 'index': 3456}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 133, 'column': 4, 'index': 3520}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 136, 'column': 4, 'index': 3622}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 136, 'column': 4, 'index': 3622}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 137, 'column': 4, 'index': 3684}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 137, 'column': 4, 'index': 3684}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 138, 'column': 4, 'index': 3743}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 139, 'column': 4, 'index': 3808}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 144, 'column': 4, 'index': 4003}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 144, 'column': 4, 'index': 4003}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 145, 'column': 4, 'index': 4065}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 145, 'column': 4, 'index': 4065}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 146, 'column': 4, 'index': 4124}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 147, 'column': 4, 'index': 4185}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 148, 'column': 4, 'index': 4244}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 169, 'column': 4, 'index': 4838}","it('should return dueDate data when required', () => {
    const newFormData = Object.assign({}, formData, {
      dueDate: {
        selectedDate: {
          date: 20,
          months: 9,
          years: 2017,
        },
        useCustom: true,
        paymentTerm: null,
      },
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          dueDate: true,
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.dueDate).toEqual({
      selectedDate: {
        date: 20,
        months: 9,
        years: 2017,
      },
      useCustom: true,
      paymentTerm: null,
    });
    expect(invoiceData.dueDate).not.toEqual({
      date: 2,
      months: 10,
      years: 2016,
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 178, 'column': 4, 'index': 5025}","it('should return dueDate data when required', () => {
    const newFormData = Object.assign({}, formData, {
      dueDate: {
        selectedDate: {
          date: 20,
          months: 9,
          years: 2017,
        },
        useCustom: true,
        paymentTerm: null,
      },
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          dueDate: true,
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.dueDate).toEqual({
      selectedDate: {
        date: 20,
        months: 9,
        years: 2017,
      },
      useCustom: true,
      paymentTerm: null,
    });
    expect(invoiceData.dueDate).not.toEqual({
      date: 2,
      months: 10,
      years: 2016,
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 557, 'column': 4, 'index': 15828}","it('should validate item quantity', () => {
    const rows = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: 0,
      },
    ];
    const validation = validateRows(rows);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });

    const rows2 = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: -1,
      },
    ];
    const validation2 = validateRows(rows2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 572, 'column': 4, 'index': 16267}","it('should validate item quantity', () => {
    const rows = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: 0,
      },
    ];
    const validation = validateRows(rows);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });

    const rows2 = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: -1,
      },
    ];
    const validation2 = validateRows(rows2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 698, 'column': 4, 'index': 19749}","it('should validate tax data', () => {
    const tax1 = { amount: 0 };
    const tax2 = { amount: '' };
    const tax3 = { amount: -1 };
    const validation1 = validateTax(true, tax1);
    expect(validation1).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation2 = validateTax(true, tax2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation3 = validateTax(true, tax3);
    expect(validation3).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 706, 'column': 4, 'index': 20018}","it('should validate tax data', () => {
    const tax1 = { amount: 0 };
    const tax2 = { amount: '' };
    const tax3 = { amount: -1 };
    const validation1 = validateTax(true, tax1);
    expect(validation1).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation2 = validateTax(true, tax2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation3 = validateTax(true, tax3);
    expect(validation3).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Duplicate Assert,"{'line': 714, 'column': 4, 'index': 20287}","it('should validate tax data', () => {
    const tax1 = { amount: 0 };
    const tax2 = { amount: '' };
    const tax3 = { amount: -1 };
    const validation1 = validateTax(true, tax1);
    expect(validation1).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation2 = validateTax(true, tax2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation3 = validateTax(true, tax3);
    expect(validation3).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Eager Test,"{'line': 129, 'column': 22, 'index': 3342}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Eager Test,"{'line': 143, 'column': 21, 'index': 3972}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Eager Test,"{'line': 555, 'column': 23, 'index': 15765}","it('should validate item quantity', () => {
    const rows = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: 0,
      },
    ];
    const validation = validateRows(rows);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });

    const rows2 = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: -1,
      },
    ];
    const validation2 = validateRows(rows2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Eager Test,"{'line': 570, 'column': 24, 'index': 16202}","it('should validate item quantity', () => {
    const rows = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: 0,
      },
    ];
    const validation = validateRows(rows);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });

    const rows2 = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: -1,
      },
    ];
    const validation2 = validateRows(rows2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Eager Test,"{'line': 696, 'column': 24, 'index': 19680}","it('should validate tax data', () => {
    const tax1 = { amount: 0 };
    const tax2 = { amount: '' };
    const tax3 = { amount: -1 };
    const validation1 = validateTax(true, tax1);
    expect(validation1).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation2 = validateTax(true, tax2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation3 = validateTax(true, tax3);
    expect(validation3).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Eager Test,"{'line': 704, 'column': 24, 'index': 19949}","it('should validate tax data', () => {
    const tax1 = { amount: 0 };
    const tax2 = { amount: '' };
    const tax3 = { amount: -1 };
    const validation1 = validateTax(true, tax1);
    expect(validation1).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation2 = validateTax(true, tax2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation3 = validateTax(true, tax3);
    expect(validation3).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Eager Test,"{'line': 712, 'column': 24, 'index': 20218}","it('should validate tax data', () => {
    const tax1 = { amount: 0 };
    const tax2 = { amount: '' };
    const tax3 = { amount: -1 };
    const validation1 = validateTax(true, tax1);
    expect(validation1).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation2 = validateTax(true, tax2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation3 = validateTax(true, tax3);
    expect(validation3).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 107, 'column': 24, 'index': 2432}","it('Should return correct data shape', () => {
    const invoiceData = getInvoiceData(formData);
    // Include custom invoiceID
    expect(invoiceData).not.toHaveProperty('invoiceID');
    // Include Rows & Recipient Data
    expect(invoiceData).toHaveProperty('rows');
    expect(invoiceData).toHaveProperty('recipient');
    expect(invoiceData).toHaveProperty('currency');
    // Not include non-required data
    expect(invoiceData).not.toHaveProperty('dueDate');
    expect(invoiceData).not.toHaveProperty('discount');
    expect(invoiceData).not.toHaveProperty('tax');
    expect(invoiceData).not.toHaveProperty('note');
    expect(invoiceData).not.toHaveProperty('settings');
    expect(invoiceData).not.toHaveProperty('savedSettings');
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 124, 'column': 24, 'index': 3186}","it('Should return rows data correctly', () => {
    const invoiceData = getInvoiceData(formData);
    expect(invoiceData.rows.length).toEqual(2);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 129, 'column': 22, 'index': 3342}","it('should return correct recipient data', () => {
    let invoiceData = getInvoiceData(formData);
    // Filter out data
    expect(invoiceData.recipient).not.toHaveProperty('new');
    expect(invoiceData.recipient).not.toHaveProperty('select');
    expect(invoiceData.recipient).not.toHaveProperty('newRecipient');

    // Choose new contact data
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).not.toHaveProperty('company');
    expect(invoiceData.recipient).not.toHaveProperty('phone');

    // Choose selected contact data
    (formData.recipient.newRecipient = false),
      (invoiceData = getInvoiceData(formData));
    expect(invoiceData.recipient).toHaveProperty('fullname');
    expect(invoiceData.recipient).toHaveProperty('email');
    expect(invoiceData.recipient).toHaveProperty('company');
    expect(invoiceData.recipient).toHaveProperty('phone');
    expect(invoiceData.recipient).toHaveProperty('id');
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 168, 'column': 24, 'index': 4805}","it('should return dueDate data when required', () => {
    const newFormData = Object.assign({}, formData, {
      dueDate: {
        selectedDate: {
          date: 20,
          months: 9,
          years: 2017,
        },
        useCustom: true,
        paymentTerm: null,
      },
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          dueDate: true,
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.dueDate).toEqual({
      selectedDate: {
        date: 20,
        months: 9,
        years: 2017,
      },
      useCustom: true,
      paymentTerm: null,
    });
    expect(invoiceData.dueDate).not.toEqual({
      date: 2,
      months: 10,
      years: 2016,
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 194, 'column': 24, 'index': 5462}","it('should return currency data when required', () => {
    const newFormData = Object.assign({}, formData, {
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          currency: true,
        }),
      }),
    });

    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.currency).toEqual({
      code: 'USD',
      placement: 'before',
      fraction: 2,
      separator: 'commaDot',
    });
    expect(invoiceData.dueDate).not.toEqual({
      code: 'VND',
      placement: 'after',
      fraction: 0,
      separator: 'spaceDot',
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 222, 'column': 24, 'index': 6225}","it('should return tax data when required', () => {
    const newFormData = Object.assign({}, formData, {
      tax: {
        amount: faker.random.number(20),
        method: 'reverse',
        tin: '123-456-789',
      },
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          tax: true,
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.tax).toEqual(newFormData.tax);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 238, 'column': 24, 'index': 6727}","it('should return discount when required', () => {
    const newFormData = Object.assign({}, formData, {
      discount: {
        amount: faker.random.number(20),
        type: 'flat',
      },
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          discount: true,
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.discount).toEqual({
      type: newFormData.discount.type,
      amount: newFormData.discount.amount,
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 256, 'column': 24, 'index': 7280}","it('should return note data when required', () => {
    const newFormData = Object.assign({}, formData, {
      note: {
        content: faker.lorem.paragraph(),
      },
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          note: true,
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.note).toEqual(newFormData.note.content);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 268, 'column': 24, 'index': 7709}","it('should return invoiceID data when required', () => {
    const newFormData = Object.assign({}, formData, {
      settings: Object.assign({}, formData.settings, {
        required_fields: Object.assign({}, formData.settings.required_fields, {
          invoiceID: true,
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData.invoiceID).toEqual('Invoice: 123-456-789');
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 290, 'column': 24, 'index': 8444}","it('should return correct metadata on editMode', () => {
    const invoiceID = uuidv4();
    const invoiceRev = uuidv4();
    const createdDate = Date.now();
    const newFormData = Object.assign({}, formData, {
      settings: Object.assign({}, formData.settings, {
        editMode: Object.assign({}, formData.settings.editMode, {
          active: true,
          data: Object.assign({}, omit(formData, ['settings, savedSettings']),
            {
              _id: invoiceID,
              _rev: invoiceRev,
              created_at: createdDate
            }
          )
        }),
      }),
    });
    const invoiceData = getInvoiceData(newFormData);
    expect(invoiceData._id).toEqual(invoiceID);
    expect(invoiceData._rev).toEqual(invoiceRev);
    expect(invoiceData.created_at).toEqual(createdDate);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 385, 'column': 23, 'index': 10762}","it('should PASS with CORRECT data', () => {
    const validation = validateFormData(formData);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 395, 'column': 23, 'index': 11066}","it('should NOT pass with INCORRECT recipient data', () => {
    const newFormData = Object.assign({}, formData, {
      recipient: Object.assign({}, formData.recipient, {
        new: {},
      }),
    });
    const validation = validateFormData(newFormData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 401, 'column': 23, 'index': 11255}","it('should NOT pass with INCORRECT rows data', () => {
    formData.rows[0].price = 0;
    const validation = validateFormData(formData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 407, 'column': 23, 'index': 11454}","it('should NOT pass with INCORRECT dueDate data', () => {
    formData.dueDate.selectedDate = null;
    const validation = validateFormData(formData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 418, 'column': 23, 'index': 11739}","it('should NOT pass with INCORRECT currency data', () => {
    formData.currency = {
      code: 'USD',
      fraction: -1,
      separator: 'commaDot',
      placement: 'before'
    };
    const validation = validateFormData(formData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 424, 'column': 23, 'index': 11931}","it('should NOT pass with INCORRECT discount data', () => {
    formData.discount.amount = 0;
    const validation = validateFormData(formData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 430, 'column': 23, 'index': 12113}","it('should NOT pass with INCORRECT tax data', () => {
    formData.tax.amount = 0;
    const validation = validateFormData(formData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 436, 'column': 23, 'index': 12299}","it('should NOT pass with INCORRECT note data', () => {
    formData.note.content = '';
    const validation = validateFormData(formData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 442, 'column': 23, 'index': 12482}","it('should NOT pass with INCORRECT invoiceID', () => {
    formData.invoiceID = '';
    const validation = validateFormData(formData);
    expect(validation).toEqual(false);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 453, 'column': 23, 'index': 12739}","it('should validate data presence', () => {
    const recipient = {
      newRecipient: true,
      new: {},
    };
    const validation = validateRecipient(recipient);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:recipient:empty:title'),
      message: i18n.t('dialog:validation:recipient:empty:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 470, 'column': 23, 'index': 13258}","it('should validate required fields data', () => {
    const recipient = {
      newRecipient: true,
      new: {
        email: faker.internet.email(),
        company: faker.company.companyName(),
      },
    };
    const validation = validateRecipient(recipient);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:recipient:requiredFields:title'),
      message: i18n.t('dialog:validation:recipient:requiredFields:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 487, 'column': 23, 'index': 13783}","it('should validate email format', () => {
    const recipient = {
      newRecipient: true,
      new: {
        fullname: faker.name.findName(),
        email: 'invalid-email-address',
      },
    };
    const validation = validateRecipient(recipient);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:recipient:email:title'),
      message: i18n.t('dialog:validation:recipient:email:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 500, 'column': 23, 'index': 14196}","it('should not validate selected contact', () => {
    const recipient = {
      newRecipient: false,
    };
    const validation = validateRecipient(recipient);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 514, 'column': 23, 'index': 14592}","it('should pass correct recipient data', () => {
    const recipient = {
      newRecipient: true,
      new: {
        fullname: faker.name.findName(),
        company: faker.company.companyName(),
        email: faker.internet.email(),
        phone: faker.phone.phoneNumber(),
      },
    };
    const validation = validateRecipient(recipient);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 522, 'column': 23, 'index': 14816}","it('should validate item description', () => {
    const rows = [{ description: '' }];
    const validation = validateRows(rows);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:emptyDescription:title'),
      message: i18n.t('dialog:validation:rows:emptyDescription:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 538, 'column': 23, 'index': 15275}","it('should validate item price', () => {
    const rows = [
      {
        description: faker.commerce.productName(),
        price: 0,
      },
    ];
    const validation = validateRows(rows);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:priceZero:title'),
      message: i18n.t('dialog:validation:rows:priceZero:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 555, 'column': 23, 'index': 15765}","it('should validate item quantity', () => {
    const rows = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: 0,
      },
    ];
    const validation = validateRows(rows);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });

    const rows2 = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: -1,
      },
    ];
    const validation2 = validateRows(rows2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:rows:qtyZero:title'),
      message: i18n.t('dialog:validation:rows:qtyZero:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 587, 'column': 23, 'index': 16713}","it('should pass correct rows data', () => {
    const rows = [
      {
        description: faker.commerce.productName(),
        price: faker.commerce.price(),
        quantity: faker.random.number(100),
      },
    ];
    const validation = validateRows(rows);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 599, 'column': 23, 'index': 16989}","it('should validate selectedDate', () => {
    const dueDate = {
      useCustom: true,
      selectedDate: null,
      paymentTerm: null,
    };
    const validation = validateDueDate(true, dueDate);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:dueDate:title'),
      message: i18n.t('dialog:validation:dueDate:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 612, 'column': 23, 'index': 17378}","it('should pass when not required', () => {
    const dueDate = {
      selectedDate: null,
    };
    const validation = validateDueDate(false, dueDate);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 620, 'column': 23, 'index': 17598}","it('should pass correct dueDate data', () => {
    const dueDate = {
      selectedDate: faker.date.future(),
    };
    const validation = validateDueDate(true, dueDate);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 633, 'column': 23, 'index': 17904}","it('should validate Currency', () => {
    const currency = {
      code: 'USD',
      fraction: -1,
      separator: 'commaDot',
      placement: 'before'
    };
    const validation = validateCurrency(true, currency);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:currency:fraction:title'),
      message: i18n.t('dialog:validation:currency:fraction:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 644, 'column': 23, 'index': 18285}","it('should pass when not required', () => {
    const currency = {};
    const validation = validateCurrency(false, currency);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 655, 'column': 23, 'index': 18562}","it('should pass correct currency data', () => {
    const currency = {
      code: 'USD',
      placement: 'before',
      fraction: 2,
      separator: 'commaDot',
    };
    const validation = validateCurrency(true, currency);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 666, 'column': 23, 'index': 18818}","it('should validate discount data', () => {
    const discount = {
      type: 'flat',
      amount: 0,
    };
    const validation = validateDiscount(true, discount);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:discount:title'),
      message: i18n.t('dialog:validation:discount:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 677, 'column': 23, 'index': 19181}","it('should pass when not required', () => {
    const discount = {};
    const validation = validateDiscount(false, discount);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 686, 'column': 23, 'index': 19402}","it('should pass correct discount data', () => {
    const discount = {
      type: 'flat',
      amount: 10,
    };
    const validation = validateDiscount(true, discount);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 696, 'column': 24, 'index': 19680}","it('should validate tax data', () => {
    const tax1 = { amount: 0 };
    const tax2 = { amount: '' };
    const tax3 = { amount: -1 };
    const validation1 = validateTax(true, tax1);
    expect(validation1).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation2 = validateTax(true, tax2);
    expect(validation2).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });

    const validation3 = validateTax(true, tax3);
    expect(validation3).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:tax:title'),
      message: i18n.t('dialog:validation:tax:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 723, 'column': 23, 'index': 20558}","it('should pass when not required', () => {
    const tax = {};
    const validation = validateTax(false, tax);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 731, 'column': 23, 'index': 20739}","it('should pass correct tax data', () => {
    const tax = {
      amount: 10,
    };
    const validation = validateTax(true, tax);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 741, 'column': 23, 'index': 20955}","it('should validate note data', () => {
    const note = {
      content: '',
    };
    const validation = validateNote(true, note);
    expect(validation).toEqual(false);
    expect(openDialog).toBeCalledWith({
      type: 'warning',
      title: i18n.t('dialog:validation:note:title'),
      message: i18n.t('dialog:validation:note:message'),
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 752, 'column': 23, 'index': 21298}","it('should pass when not required', () => {
    const note = {};
    const validation = validateNote(false, note);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 760, 'column': 23, 'index': 21505}","it('should pass correct note data', () => {
    const note = {
      content: faker.lorem.paragraph(),
    };
    const validation = validateNote(true, note);
    expect(validation).toEqual(true);
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 795, 'column': 26, 'index': 22479}","it('should return current contact if it exist', () => {
    const currentContact = allContacts[1];
    const editRecipient = setEditRecipient(allContacts, currentContact);
    expect(editRecipient).toEqual({
      newRecipient: false,
      select: currentContact,
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Lazy Test,"{'line': 810, 'column': 26, 'index': 22966}","it('should create a new contact if the current contact does not exist', () => {
    const currentContact = {
      _id: 'random-string',
      fullname: faker.name.findName(),
      email: faker.internet.email(),
      company: faker.company.companyName(),
      phone: faker.phone.phoneNumber(),
    };
    const editRecipient = setEditRecipient(allContacts, currentContact);
    expect(editRecipient).toEqual({
      newRecipient: true,
      new: {
        fullname: currentContact.fullname,
        email: currentContact.email,
        company: currentContact.company,
        phone: currentContact.phone,
      },
    });
  })",steel
/app/helpers/__tests__/form.spec.js,Magic Number,"{'line': 125, 'column': 44, 'index': 3256}","it('Should return rows data correctly', () => {
    const invoiceData = getInvoiceData(formData);
    expect(invoiceData.rows.length).toEqual(2);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Duplicate Assert,"{'line': 76, 'column': 4, 'index': 2009}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Duplicate Assert,"{'line': 76, 'column': 4, 'index': 2009}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Duplicate Assert,"{'line': 77, 'column': 4, 'index': 2064}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Duplicate Assert,"{'line': 77, 'column': 4, 'index': 2064}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Duplicate Assert,"{'line': 114, 'column': 4, 'index': 3128}","it('set correct filter', () => {
    expect(spySetFilter).toHaveBeenCalled();
    const filter = wrapper.state().filter;
    expect(filter).toEqual('paid');
    expect(filter).not.toEqual('pending');
  })",steel
/app/containers/__tests__/Invoices.spec.js,Duplicate Assert,"{'line': 115, 'column': 4, 'index': 3164}","it('set correct filter', () => {
    expect(spySetFilter).toHaveBeenCalled();
    const filter = wrapper.state().filter;
    expect(filter).toEqual('paid');
    expect(filter).not.toEqual('pending');
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 68, 'column': 53, 'index': 1720}","it('render empty message when there is no invoice in the DB', () => {
    const newWrapper = mount(
      <Invoices t={t} invoices={[]} dispatch={dispatch} />
    );
    expect(newWrapper.find('.message')).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 72, 'column': 51, 'index': 1827}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 73, 'column': 50, 'index': 1881}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 74, 'column': 55, 'index': 1940}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 75, 'column': 57, 'index': 2001}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 76, 'column': 51, 'index': 2056}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 77, 'column': 51, 'index': 2111}","it('render a correct page layout', () => {
    expect(wrapper.find(PageWrapper)).toHaveLength(1);
    expect(wrapper.find(PageHeader)).toHaveLength(1);
    expect(wrapper.find(PageHeaderTitle)).toHaveLength(1);
    expect(wrapper.find(PageHeaderActions)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
    expect(wrapper.find(PageContent)).toHaveLength(1);
  })",steel
/app/containers/__tests__/Invoices.spec.js,Magic Number,"{'line': 82, 'column': 52, 'index': 2290}","it('renders filter with four options', () => {
    const HeaderActions = wrapper.find(PageHeaderActions).first();
    expect(HeaderActions.find(Button)).toHaveLength(4);
  })",steel
/app/components/shared/_tests_/SplitButton.spec.js,Magic Number,"{'line': 45, 'column': 43, 'index': 960}","it('renders links as a element', () => {
    expect(wrapper.find('a')).toHaveLength(2);
    expect(wrapper.find('button')).toHaveLength(0);
  })",steel
/app/components/shared/_tests_/SplitButton.spec.js,Magic Number,"{'line': 46, 'column': 48, 'index': 1012}","it('renders links as a element', () => {
    expect(wrapper.find('a')).toHaveLength(2);
    expect(wrapper.find('button')).toHaveLength(0);
  })",steel
/app/components/shared/_tests_/SplitButton.spec.js,Magic Number,"{'line': 51, 'column': 60, 'index': 1179}","it('hide options by default', () => {
    expect(wrapper.state().showOptions).toEqual(false);
    expect(wrapper.find('SplitButton__Addon')).toHaveLength(0);
  })",steel
/app/components/shared/_tests_/SplitButton.spec.js,Magic Number,"{'line': 57, 'column': 60, 'index': 1385}","it('renders options list correctly', () => {
    toggleBtn.simulate('click');
    expect(wrapper.state().showOptions).toEqual(true);
    expect(wrapper.find('SplitButton__Addon')).toHaveLength(1);
    expect(wrapper.find('SplitButton__Addon').children()).toHaveLength(
      options.length
    );
  })",steel
/app/components/shared/_tests_/SplitButton.spec.js,Magic Number,"{'line': 117, 'column': 60, 'index': 3173}","it('handle click outside correctly', () => {
    // Show the dropdown
    toggleBtn.simulate('click');
    // Click the main button - outside the option
    mainBtn.simulate('click');
    // The dropdown will be removed
    expect(wrapper.state().showOptions).toEqual(false);
    expect(wrapper.find('SplitButton__Addon')).toHaveLength(0);
  })",steel
/app/components/shared/_tests_/Section.spec.js,Magic Number,"{'line': 11, 'column': 45, 'index': 292}","it('renders a DIV element', () => {
    const section = mount(<Section />);
    expect(section.find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Section.spec.js,Magic Number,"{'line': 23, 'column': 45, 'index': 550}","it('renders its children', () => {
    const section = mount(
      <Section>
        <div>
          <h4>Hello, World</h4>
          <p>This is a paragraph</p>
        </div>
      </Section>
    );
    expect(section.find('div')).toHaveLength(2);
    expect(section.find('h4')).toHaveLength(1);
    expect(section.find('p')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Section.spec.js,Magic Number,"{'line': 24, 'column': 44, 'index': 598}","it('renders its children', () => {
    const section = mount(
      <Section>
        <div>
          <h4>Hello, World</h4>
          <p>This is a paragraph</p>
        </div>
      </Section>
    );
    expect(section.find('div')).toHaveLength(2);
    expect(section.find('h4')).toHaveLength(1);
    expect(section.find('p')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Section.spec.js,Magic Number,"{'line': 25, 'column': 43, 'index': 645}","it('renders its children', () => {
    const section = mount(
      <Section>
        <div>
          <h4>Hello, World</h4>
          <p>This is a paragraph</p>
        </div>
      </Section>
    );
    expect(section.find('div')).toHaveLength(2);
    expect(section.find('h4')).toHaveLength(1);
    expect(section.find('p')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Message.spec.js,Magic Number,"{'line': 20, 'column': 45, 'index': 588}","it('renders a DIV element', () => {
    const message = mount(<Message text=""foo"" />);
    expect(message.find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 19, 'column': 59, 'index': 398}","it('AppWrapper renders a DIV element', () => {
    expect(mount(<AppWrapper />).find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 22, 'column': 63, 'index': 524}","it('AppMainContent renders a DIV element', () => {
    expect(mount(<AppMainContent />).find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 25, 'column': 60, 'index': 644}","it('PageWrapper renders a DIV element', () => {
    expect(mount(<PageWrapper />).find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 28, 'column': 59, 'index': 762}","it('PageHeader renders a DIV element', () => {
    expect(mount(<PageHeader />).find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 31, 'column': 62, 'index': 888}","it('PageHeaderTitle renders a DIV element', () => {
    expect(mount(<PageHeaderTitle />).find('p')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 34, 'column': 66, 'index': 1020}","it('PageHeaderActions renders a DIV element', () => {
    expect(mount(<PageHeaderActions />).find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 37, 'column': 60, 'index': 1140}","it('PageContent renders a DIV element', () => {
    expect(mount(<PageContent />).find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 40, 'column': 59, 'index': 1258}","it('PageFooter renders a DIV element', () => {
    expect(mount(<PageFooter />).find('div')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 51, 'column': 48, 'index': 1526}","it('AppWrapper renders its children', () => {
    const appWrapper = mount(
      <AppWrapper>
        <div>Hello, World</div>
      </AppWrapper>
    );
    expect(appWrapper.find('div')).toHaveLength(2);
    expect(appWrapper.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 61, 'column': 52, 'index': 1816}","it('AppMainContent renders its children', () => {
    const appMainContent = mount(
      <AppMainContent>
        <div>Hello, World</div>
      </AppMainContent>
    );
    expect(appMainContent.find('div')).toHaveLength(2);
    expect(appMainContent.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 71, 'column': 49, 'index': 2095}","it('PageWrapper renders its children', () => {
    const pageWrapper = mount(
      <PageWrapper>
        <div>Hello, World</div>
      </PageWrapper>
    );
    expect(pageWrapper.find('div')).toHaveLength(2);
    expect(pageWrapper.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 81, 'column': 48, 'index': 2366}","it('PageHeader renders its children', () => {
    const pageHeader = mount(
      <PageHeader>
        <div>Hello, World</div>
      </PageHeader>
    );
    expect(pageHeader.find('div')).toHaveLength(2);
    expect(pageHeader.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 91, 'column': 51, 'index': 2662}","it('PageHeaderTitle renders a DIV element', () => {
    const pageHeaderTitle = mount(
      <PageHeaderTitle>
        <span>Hello, World</span>
      </PageHeaderTitle>
    );
    expect(pageHeaderTitle.find('p')).toHaveLength(1);
    expect(pageHeaderTitle.find('span')).toHaveLength(1);
    expect(pageHeaderTitle.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 92, 'column': 54, 'index': 2720}","it('PageHeaderTitle renders a DIV element', () => {
    const pageHeaderTitle = mount(
      <PageHeaderTitle>
        <span>Hello, World</span>
      </PageHeaderTitle>
    );
    expect(pageHeaderTitle.find('p')).toHaveLength(1);
    expect(pageHeaderTitle.find('span')).toHaveLength(1);
    expect(pageHeaderTitle.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 102, 'column': 55, 'index': 3030}","it('PageHeaderActions renders its children', () => {
    const pageHeaderActions = mount(
      <PageHeaderActions>
        <div>Hello, World</div>
      </PageHeaderActions>
    );
    expect(pageHeaderActions.find('div')).toHaveLength(2);
    expect(pageHeaderActions.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 112, 'column': 49, 'index': 3312}","it('PageContent renders its children', () => {
    const pageContent = mount(
      <PageContent>
        <div>Hello, World</div>
      </PageContent>
    );
    expect(pageContent.find('div')).toHaveLength(2);
    expect(pageContent.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Layout.spec.js,Magic Number,"{'line': 122, 'column': 48, 'index': 3583}","it('PageFooter renders its children', () => {
    const pageFooter = mount(
      <PageFooter>
        <div>Hello, World</div>
      </PageFooter>
    );
    expect(pageFooter.find('div')).toHaveLength(2);
    expect(pageFooter.text()).toEqual('Hello, World');
  })",steel
/app/components/shared/_tests_/Button.spec.js,Magic Number,"{'line': 11, 'column': 42, 'index': 285}","it('renders a Link(a) element', () => {
    const button = mount(<Button />);
    expect(button.find('a')).toHaveLength(0);
    expect(button.find('button')).toHaveLength(1);
  })",steel
/app/components/shared/_tests_/Button.spec.js,Magic Number,"{'line': 12, 'column': 47, 'index': 336}","it('renders a Link(a) element', () => {
    const button = mount(<Button />);
    expect(button.find('a')).toHaveLength(0);
    expect(button.find('button')).toHaveLength(1);
  })",steel
/app/components/layout/__tests__/AppNav.spec.js,Empty Test,"{'line': 21, 'column': 2, 'index': 454}","it('has correct porps', () => {
    // expect(wrapper.prop('activeTab')).toEqual('form');
    // expect(wrapper.prop('activeTab')).not.toEqual('settings');
  })",steel
/app/components/layout/__tests__/AppNav.spec.js,Empty Test,"{'line': 26, 'column': 2, 'index': 619}","it('contains active indicator', () => {
  //   const sideBar = shallow(<ActiveIndicator/>);
  //   expect(wrapper.contains(sideBar)).toEqual(true);
  })",steel
/app/components/layout/__tests__/AppNav.spec.js,Magic Number,"{'line': 18, 'column': 43, 'index': 441}","it('renders 4 tabs', () => {
    expect(wrapper.find('a')).toHaveLength(4);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Conditional Test Logic,"{'line': 44, 'column': 2, 'index': 1057}",Unknown,steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 97, 'column': 4, 'index': 2413}","it('render with a class of .col-lg-6', () => {
    expect(wrapper.find('.col-lg-6')).toHaveLength(1);
    expect(wrapper.find('.col-lg-6')).not.toHaveLength(2);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 98, 'column': 4, 'index': 2468}","it('render with a class of .col-lg-6', () => {
    expect(wrapper.find('.col-lg-6')).toHaveLength(1);
    expect(wrapper.find('.col-lg-6')).not.toHaveLength(2);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 256, 'column': 4, 'index': 6832}","it('handle edit action correctly', () => {
    editBtn.simulate('click');
    expect(editInvoice).toHaveBeenCalled();
    expect(editInvoice).toHaveBeenCalledWith(invoice);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 257, 'column': 4, 'index': 6876}","it('handle edit action correctly', () => {
    editBtn.simulate('click');
    expect(editInvoice).toHaveBeenCalled();
    expect(editInvoice).toHaveBeenCalledWith(invoice);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 262, 'column': 4, 'index': 7018}","it('handle delete action correctly', () => {
    deleteBtn.simulate('click');
    expect(deleteInvoice).toHaveBeenCalled();
    expect(deleteInvoice).toHaveBeenCalledWith(invoice._id);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 263, 'column': 4, 'index': 7064}","it('handle delete action correctly', () => {
    deleteBtn.simulate('click');
    expect(deleteInvoice).toHaveBeenCalled();
    expect(deleteInvoice).toHaveBeenCalledWith(invoice._id);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 268, 'column': 4, 'index': 7218}","it('handle duplicate action correctly', () => {
    duplicateBtn.simulate('click');
    expect(duplicateInvoice).toHaveBeenCalled();
    expect(duplicateInvoice).toHaveBeenCalledWith(invoice);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Duplicate Assert,"{'line': 269, 'column': 4, 'index': 7267}","it('handle duplicate action correctly', () => {
    duplicateBtn.simulate('click');
    expect(duplicateInvoice).toHaveBeenCalled();
    expect(duplicateInvoice).toHaveBeenCalledWith(invoice);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Magic Number,"{'line': 97, 'column': 51, 'index': 2460}","it('render with a class of .col-lg-6', () => {
    expect(wrapper.find('.col-lg-6')).toHaveLength(1);
    expect(wrapper.find('.col-lg-6')).not.toHaveLength(2);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Magic Number,"{'line': 98, 'column': 55, 'index': 2519}","it('render with a class of .col-lg-6', () => {
    expect(wrapper.find('.col-lg-6')).toHaveLength(1);
    expect(wrapper.find('.col-lg-6')).not.toHaveLength(2);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Magic Number,"{'line': 104, 'column': 19, 'index': 2675}","it('render invoice status in the header', () => {
    expect(
      wrapper.find('Invoice__Header').find('Invoice__Status')
    ).toHaveLength(1);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Magic Number,"{'line': 190, 'column': 70, 'index': 5029}","it('render delete and duplicate button in the header', () => {
    expect(wrapper.find('Invoice__Header').find(Button)).toHaveLength(2);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Magic Number,"{'line': 194, 'column': 75, 'index': 5181}","it('render a split button and 2 buttons in the footer', () => {
    expect(wrapper.find('Invoice__Footer').find(SplitButton)).toHaveLength(1);
    expect(wrapper.find('Invoice__Footer').find(Button)).toHaveLength(2);
  })",steel
/app/components/invoices/__tests__/Invoice.spec.js,Magic Number,"{'line': 195, 'column': 70, 'index': 5255}","it('render a split button and 2 buttons in the footer', () => {
    expect(wrapper.find('Invoice__Footer').find(SplitButton)).toHaveLength(1);
    expect(wrapper.find('Invoice__Footer').find(Button)).toHaveLength(2);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 34, 'column': 4, 'index': 722}","it('renders 1 TR row', () => {
    expect(wrapper.find(TR)).toHaveLength(1);
    expect(wrapper.find(TR)).not.toHaveLength(2);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 35, 'column': 4, 'index': 768}","it('renders 1 TR row', () => {
    expect(wrapper.find(TR)).toHaveLength(1);
    expect(wrapper.find(TR)).not.toHaveLength(2);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 39, 'column': 4, 'index': 862}","it('renders 4 TD columns', () => {
    expect(wrapper.find(TD)).toHaveLength(4);
    expect(wrapper.find(TD)).not.toHaveLength(5);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 40, 'column': 4, 'index': 908}","it('renders 4 TD columns', () => {
    expect(wrapper.find(TD)).toHaveLength(4);
    expect(wrapper.find(TD)).not.toHaveLength(5);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 57, 'column': 4, 'index': 1413}","it('renders 2 buttons in the last TD', () => {
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).toHaveLength(2);
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).not.toHaveLength(3);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 63, 'column': 4, 'index': 1518}","it('renders 2 buttons in the last TD', () => {
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).toHaveLength(2);
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).not.toHaveLength(3);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 89, 'column': 6, 'index': 2270}","it('handle click correctly', () => {
      firstBtn.simulate('click');
      expect(newInvoice).toHaveBeenCalled();
      expect(newInvoice).toHaveBeenCalledWith(contact);

      lastBtn.simulate('click');
      expect(deleteContact).toHaveBeenCalled();
      expect(deleteContact).toHaveBeenCalledWith('id-string');
    })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 90, 'column': 6, 'index': 2315}","it('handle click correctly', () => {
      firstBtn.simulate('click');
      expect(newInvoice).toHaveBeenCalled();
      expect(newInvoice).toHaveBeenCalledWith(contact);

      lastBtn.simulate('click');
      expect(deleteContact).toHaveBeenCalled();
      expect(deleteContact).toHaveBeenCalledWith('id-string');
    })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 93, 'column': 6, 'index': 2405}","it('handle click correctly', () => {
      firstBtn.simulate('click');
      expect(newInvoice).toHaveBeenCalled();
      expect(newInvoice).toHaveBeenCalledWith(contact);

      lastBtn.simulate('click');
      expect(deleteContact).toHaveBeenCalled();
      expect(deleteContact).toHaveBeenCalledWith('id-string');
    })",steel
/app/components/contacts/__tests__/Contact.spec.js,Duplicate Assert,"{'line': 94, 'column': 6, 'index': 2453}","it('handle click correctly', () => {
      firstBtn.simulate('click');
      expect(newInvoice).toHaveBeenCalled();
      expect(newInvoice).toHaveBeenCalledWith(contact);

      lastBtn.simulate('click');
      expect(deleteContact).toHaveBeenCalled();
      expect(deleteContact).toHaveBeenCalledWith('id-string');
    })",steel
/app/components/contacts/__tests__/Contact.spec.js,Magic Number,"{'line': 34, 'column': 42, 'index': 760}","it('renders 1 TR row', () => {
    expect(wrapper.find(TR)).toHaveLength(1);
    expect(wrapper.find(TR)).not.toHaveLength(2);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Magic Number,"{'line': 35, 'column': 46, 'index': 810}","it('renders 1 TR row', () => {
    expect(wrapper.find(TR)).toHaveLength(1);
    expect(wrapper.find(TR)).not.toHaveLength(2);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Magic Number,"{'line': 39, 'column': 42, 'index': 900}","it('renders 4 TD columns', () => {
    expect(wrapper.find(TD)).toHaveLength(4);
    expect(wrapper.find(TD)).not.toHaveLength(5);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Magic Number,"{'line': 40, 'column': 46, 'index': 950}","it('renders 4 TD columns', () => {
    expect(wrapper.find(TD)).toHaveLength(4);
    expect(wrapper.find(TD)).not.toHaveLength(5);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Magic Number,"{'line': 62, 'column': 19, 'index': 1510}","it('renders 2 buttons in the last TD', () => {
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).toHaveLength(2);
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).not.toHaveLength(3);
  })",steel
/app/components/contacts/__tests__/Contact.spec.js,Magic Number,"{'line': 68, 'column': 23, 'index': 1619}","it('renders 2 buttons in the last TD', () => {
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).toHaveLength(2);
    expect(
      wrapper
        .find(TD)
        .last()
        .find(Button)
    ).not.toHaveLength(3);
  })",steel
